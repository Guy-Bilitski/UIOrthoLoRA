# Immutable numerical audit fixtures

These three files were exported byte-for-byte from Overleaf commit `9c8043c1b531f3afb27fa55ce101c8635736c938`. Their relative paths and SHA-256 digests are in `manifest.json`. They are historical table fixtures, not active manuscript sections or new experiment outputs.

The manuscript audit checks their hashes and uses them to verify preservation of all historical GLUE means/SDs, generation values and archived retention values. This makes the checks runnable in a normal GitHub clone with no Overleaf history or credentials. Never edit these fixtures to make a changed result pass; reconcile new evidence with explicit provenance instead.
