Latest scoped review: Overleaf `54f5c706280863258a3d407d866e06dd38475224`; see
`DECODER_SCOPE_REVIEW_20260919.md` and `DECODER_SCOPE_REVIEW_SYNC_20260919.json`.
Reviewed coding progress through `4d7eb8bc`; no implementation, measured artifact,
scientific manuscript or live queue was changed by this review. The prior
subspace-plan delivery below remains provenance of the superseded tuning plan.

Latest scoped subspace-design update: Overleaf `cb449aced2f23f5bc620dc9a4b015ac1948fb477`. See
`DECODER_SUBSPACE_STUDY_20260919.md` and `DECODER_SUBSPACE_SYNC_20260919.json`.
This delivery preserves the intervening coding-agent commit `0fddf62e` and
Overleaf `56e4e6a`; their old benchmark registration must be adapted to the
current subspace question. It changes planning documents only, not implementation,
measured data, approved manuscript or existing PDF/supplement contents.

Latest scoped priority update: Overleaf `070bcefd173cbfe0a3f4455a7da360ea174008f2`. See
`DECODER_FIRST_PRIORITY_20260919.md` and `DECODER_PRIORITY_SYNC_20260919.json`.
The original full-snapshot manifest below records its earlier delivery, not a
promise that later coding-session exports and status documents are unchanged.

# GitHub delivery provenance — 19 September 2026

The current entry point is `EXPERIMENT_PREPARATION_HANDOFF_20260919.md`.
This scoped delivery is on the existing `ortho_new` branch of
`Guy-Bilitski/UIOrthoLoRA`; other-project files and shared working trees are untouched.

The snapshot copies every tracked file from Overleaf project
`6aa54397e58b10444b0fa2aa`, commit `df8cdad8c56e8f4fd5a745b1fda8619087d68a07`.
Scientific manuscript content is unchanged from `932283e1`; the later commit
publishes the feedback assessment, diagnostic reproducer, PDF, refreshed ZIP and
current preparation handoff. `SOURCE_SNAPSHOT_20260919.json` records original and
copied SHA256 values for every file. Only AGENTS.md has an added GitHub-location
preamble; all other copied files match their Overleaf source bytes.

Research-side operator scripts, server handoffs and hash-verified historical
fixtures in `audit_baseline/` remain available alongside the snapshot. These are
not overwritten by unrelated files from another project. The older session
handoff is retained with an explicit historical-status banner. The history's
four-GPU instructions do not override the author's current two-GPU budget.

The same manuscript, figures, data, recorded experiment source, CPU analysis
scripts, feedback assessment, PDF and anonymous supplement are now accessible
from either repository. The current trained-model evidence remains 18 practical
confirmations with held-aside evaluation and 21 band/head confirmations with
inner-selection scores. New proposals and descriptive raw calibration analysis
are identified separately; this delivery launches no inference or training.

Run CPU paper/analysis checks from this directory; `audit_baseline/` supplies
historical fixtures without requiring Overleaf Git history. Rebuild the anonymous
supplement from the actual Overleaf checkout, since its builder accesses that
project's historical revisions. The existing ZIP is already verified and its
source matches this snapshot. Internal handoffs and assessment files are not
anonymous submission material. The ZIP does not contain checkpoints or dataset
text; use the frozen population manifests to locate durable server artifacts.

Fetch/reconcile the current remote before any later scoped commit and normal
push. The research remote is not the Overleaf bridge. Never force-push or replace
current author edits with this snapshot without reconciliation.
