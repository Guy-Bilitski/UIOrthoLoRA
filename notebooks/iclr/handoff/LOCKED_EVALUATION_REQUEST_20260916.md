# One-time held-aside evaluation request — 2026-09-16

No new training is requested. Evaluate all 18 already frozen fixed-endpoint P1 confirmation checkpoints enumerated in `data/locked_evaluation_request_20260916.json`, once, on their official locked splits (RTE 277 examples; MRPC 408). Use the repaired input preparation and the existing validated recipe and checkpoint. Exclude calibration, timing pilots, invalidated runs and best-inner-selected checkpoints from this primary request.

Export a separately labeled held-aside CSV: task, condition, seed, run ID, checkpoint identifier and SHA256, fixed optimizer step, accuracy, MRPC F1, loss, example count, split/input/preparation hashes, source revision, evaluation-manifest hash and validation-report hash. Preserve per-example IDs, labels, predictions and loss sums for independent aggregation, with no raw dataset text required. Reproduce the frozen checkpoint's inner metrics as a loading check. Do not choose checkpoints, change doses, tune thresholds, select seeds, or rerun training based on held-aside outcomes. Retain all results regardless of direction. Record any implementation-only evaluation correction and preserve its first output.

For block B, the same endpoint policy applies prospectively to all 21 registered RTE confirmation entries (six arms × seeds 17/42/123 and three head-only references). First freeze their completed validated run/checkpoint IDs against protocol SHA256 `721935221ca1b4db38736016fbb256fa1d355d4dcf8a90a693856eaf7614fdeb`; evaluate the complete block together after it finishes. Do not substitute the two timing pilots or condition completion on held-aside performance. This request does not alter resource allocation or authorize additional training.

Report held-aside scores alongside, never merged with, inner-selection scores or the historical GLUE table. Use seed means and sample SD, every paired seed contrast already specified, and descriptive nominal paired intervals only. The fixed example set and three seeds do not support task-general superiority or equivalence claims.
