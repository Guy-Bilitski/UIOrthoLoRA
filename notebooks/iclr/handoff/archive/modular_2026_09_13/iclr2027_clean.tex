% Inactive modular snapshot; edit the root neurips_2026.tex instead.
% Clean anonymous build; not a camera-ready/de-anonymized build.
% Compatibility entry: the approved manuscript is now clean by default.
\input{iclr2027}
