# Handoff 40 — Resume prompt + live state (2026-07-15 ~08:52Z / 11:52 local UTC+3)

Continues handoff/39 (fleet eval recovery). Read 39 first for the incident detail. This file = current
state + the paste-in resume prompt for the next agent. **The fleet keeps running without an agent** (detached
bash infra on the nodes); an agent is only the supervisor/reporter.

## Live state (snapshot)
- **Topology:** 30-node × 8×B200 fleet. This host = `d001` (head). Remotes `d002`–`d032` (`fleet/ready_nodes.txt`).
  Each node runs `auto_dispatch.py --jobs jobs/fleet/<hostname>.txt … --hf_offline 1` (train_cs && eval per cell),
  self-healed by per-node `gpu_watchdog.sh`. d001 also runs `guardian_loop.sh` (revive dead dispatchers + lock GC
  + auto-finalize at 90%) and `collect_loop.sh` (rsync all nodes → d001 → git push, ~20 min).
- **Health:** all 30 dispatchers UP, ~8/8 GPUs busy; guardian + collect UP on d001. gsm8k offline eval works
  fleet-wide (verified). Latest sync commit at snapshot: `cfe4d806` (08:36Z).
- **Progress:** **5 / 714** planned cells have summaries. **275 adapters trained & banked** on disk fleet-wide
  (~270 need only eval — training preserved, NOT re-trained). The rest (~439) need full train+eval.
- **ETA:** ~12–18h from 08:52Z → roughly late 2026-07-15 → morning 2026-07-16 UTC. Deadline ~Sunday, comfortable.
  Eval time is the dominant unknown (Llama retention-broad ~1–1.5h, Qwen ~2.5h); refine from steady-state throughput.

## What was fixed today (all committed to ortho_new + pushed)
- `bbf5eb8f` — `train_cs.py` idempotency guard: if a completed adapter exists (`run_config.json`+
  `adapter_model.safetensors`), SKIP train and let `&& eval` run. Live on all 30 nodes. Recovers the 270 banked
  adapters as eval-only instead of hours of retrain.
- `0e522023` — handoff/39 incident writeup + STATUS.md ops banner.
- `816a098f` — committed the previously-untracked `fleet/` dir; distributed `finalize_node.sh` to all 30 nodes
  (was missing on remotes → auto-finalize would have silently no-op'd); added `openai/gsm8k` to `precache_datasets.sh`.
- Root cause of "evals didn't work": `openai/gsm8k` was never cached and dispatch is offline → every
  `--adapt_task gsm8k` cell trained for hours then died at eval. Fixed by caching on d001 (only node with egress)
  and rsyncing `/scratch/hf_cache/datasets/openai___gsm8k/` to all nodes. Also reverted ~48 invalid eval-only
  shard edits (restored `jobs/fleet/*.txt` from `*.txt.bak`).

## Operational rules / gotchas (learned the hard way)
- **Timezone:** box is **UTC+3**. `nvidia-smi`/`date` print local; logs stamp local (`+0300`); git commit msgs use Z.
  Don't compare a local clock to a Z timestamp (cost me a wrong "loops wedged" call earlier).
- `/home/guy/UIOrthoLoRA` and `/home/guyb/UIOrthoLoRA` are the **same files** (same inode). Venv `/home/guy/.../.venv`.
- **d001 dispatcher runs as root** (locks + cf_models root-owned → use `sudo` on d001); **remote dispatchers run as
  ubuntu** (no sudo on remotes; `ssh ubuntu@dNNN`).
- **Only d001 has internet egress.** Cache datasets on d001, then rsync the cache dir to nodes.
- Dispatcher reads its jobs file **once at startup**; shard edits only affect future (re)launches.
- Kill by **PID, never `pkill -f`**; kill and relaunch are **separate** calls; launch detached with **setsid**.
- Never `git add -A` (results churn + lock dirs). `fleet/collect.sh` scopes to `results/` and excludes lock dirs.
- Session-only crons (CronCreate) **die with the session** — the last agent set one (`389c891b` :17 hourly); it's
  now gone. Recreate it (below) or use a durable cloud routine for overnight.

## DO NOT (without PI go-ahead)
- Touch `paper.tex` (PI-paused) or publish/modify the artifact (live URL in handoff/38 §4; you'll mint a duplicate).
- Kill running training to "speed up" — the guard already prevents retrain waste; let cells finish.

## ── PASTE-IN RESUME PROMPT (for the next agent) ──────────────────────────────
You are resuming as autonomous supervisor of the UIOrthoLoRA magnitude-law campaign on a live 30-node ×
8×B200 fleet (this host = d001; remotes d002–d032 in fleet/ready_nodes.txt). Working dir:
/home/guy/UIOrthoLoRA/notebooks/catastrophic forgetting. FIRST: read handoff/40_RESUME_2026-07-15.md end-to-end
(state, fixes, ops rules) then handoff/39. The fleet self-runs via detached bash infra (per-node
gpu_watchdog.sh; d001 guardian_loop.sh + collect_loop.sh) — your job is to supervise it to full completion of
all 714 cells + per-node geometry/CE finalize, DATA-ONLY. Do NOT touch paper.tex (PI-paused) or the artifact.

Do NOW: (1) Verify health — all 30 nodes have a live `auto_dispatch.py --jobs jobs/fleet` and ~8/8 GPUs busy;
if any DOWN, `bash fleet/relaunch_all.sh` (idempotent, setsid). Confirm guardian_loop.sh + collect_loop.sh alive
on d001 (relaunch detached via setsid if not; kill by PID, never pkill -f; kill and relaunch as separate calls).
(2) Sync — `sudo git -C /home/guy/UIOrthoLoRA log --oneline -1`; if collect hasn't pushed in >30 min run
`sudo bash fleet/collect.sh`. (3) GC — clear any dispatch_locks/*.lock with no summary.json + no live `run_name`
proc + >8 min old (guardian logic); kill any compute proc whose per-job log mtime is >90 min old at high GPU util
(spinning wedge; GC requeues). (4) Progress + ETA — planned = unique --run_name across jobs/fleet/d*.txt (=714);
done = matching summaries on d001; report done/714, banked-adapters-still-needing-eval, and an ETA from the
last-hour completion rate. (5) Recreate the hourly session supervisor cron (session crons die on disconnect):
CronCreate `17 * * * *` with a DATA-ONLY prompt doing steps 1–4 + a fresh-Traceback grep (ignore
gsm8k/OfflineModeIsEnabled — fixed), reporting one short paragraph, and saying "FLEET COMPLETE" when done/714==714
and every node has results/finalize_<node>.done.

Key facts: box is UTC+3 (logs local, commits Z — don't mix). /home/guy==/home/guyb (same inode); venv
/home/guy/UIOrthoLoRA/.venv. d001 = root (use sudo); remotes = ubuntu (ssh ubuntu@dNNN, no sudo). Only d001 has
internet egress (cache on d001, rsync cache dir to nodes). Dispatcher reads jobs once at startup. Already fixed +
committed (ortho_new): gsm8k offline cache fleet-wide, train_cs.py skip-guard (bbf5eb8f), finalize_node.sh
distributed + fleet/ tracked (816a098f). Standing orders: keep all 240 GPUs busy, sync to GitHub every wake-up
(nothing lives only on hosts), report honestly incl. failures. At full completion, report the final done/714 and
that finalize batteries (geometry + CE) are present per node — then ask the PI before any artifact/paper work.
## ─────────────────────────────────────────────────────────────────────────────
