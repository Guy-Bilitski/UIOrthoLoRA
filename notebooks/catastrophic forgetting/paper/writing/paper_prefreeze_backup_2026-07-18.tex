\documentclass[11pt]{article}

% ---------------------------------------------------------------------------
% "The Magnitude Law: Update Size Is the First-Order Predictor of Forgetting
%  in PEFT" (geometry = second-order fingerprint; rank = modest second lever)
%
% Round-4 manuscript. Derived from the verified baseline paper_draft.tex; DO NOT
% overwrite that file. Numbers from data/key_numbers.md (single source of truth,
% sections 14-15 for the split LoRA-Null convention; Llama-2-7B seed 42 unless
% marked Qwen). Pending-experiment claims carry visible %TODO markers.
%
% Claim order (renumbered 2026-07-02): 1 Mechanism (law) -> 2 Diagnosis (LR
% artifact) -> 3 Consequence (LoRA+wd corollary) -> Message.
%
% Figures live in ./figures/*.png (regenerated 2026-07-02, second pass, split
% LoRA-Null convention via make_figs_split_lora_null.py; frozen originals in
% figures_frozen_backup/). Tables in ./tables/*.tex. Paper-local hand-edited
% variants (generated assets untouched): table_lr_artifact_paper.tex (CorDA row
% dropped), table_main_cs_paper.tex (LoRA-Null row + robustness column),
% table_op_points.tex (E7), table_claims_evidence.tex (claims map).
%
% Layout: argument-carrying figures in the body (fig1 axis, fig0 hero, fig2
% ANCOVA residuals, fig7 proxy, fig9 LR-artifact, fig8 budget, fig3 best points);
% supporting figures (fig4/fig5/fig6) + operating-point table + compute statement
% in the appendix.
% ---------------------------------------------------------------------------

% ACL/ARR style (review mode: anonymized, line numbers). Change to [final] for
% camera-ready or [preprint] for arXiv. acl.sty loads natbib + hyperref itself.
\usepackage[review]{acl}
\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{inconsolata}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{amsfonts}
\usepackage{xcolor}

\graphicspath{{figures/}}

% Magnitude axis: this is CLoRA's F_Delta diagnostic (their Eq 3), the mean
% normalized output displacement ||dW x||/||x||, NOT the raw Frobenius norm. The
% registry field is `fdelta`. Relabeled 2026-07-09 per key_numbers.md sec 0 (a
% CLoRA-reading reviewer would catch the old "Frobenius" label; F_Delta is also
% directly comparable to CLoRA Table 4). Macro name kept as \dw for minimal churn.
\newcommand{\dw}{F_{\Delta}}
\newcommand{\smax}{\sigma_{\max}}

\title{The Magnitude Law: Update Size Is the First-Order Predictor of\\
Forgetting in Parameter-Efficient Fine-Tuning}

\author{Anonymous}

\begin{document}
\maketitle

% ===========================================================================
\begin{abstract}
% ===========================================================================
The literature ships a new parameter-efficient fine-tuning (PEFT) adapter almost
every week, each promising to mitigate catastrophic forgetting through a clever
\emph{geometric} prior: a data-aware initialization, a null-space projection, a
spectral decomposition, an orthogonality penalty. We argue that the
\emph{first-order} governing variable is the magnitude of the update, and that
geometry acts largely \emph{through} that magnitude rather than beside it, and we
make the case with two instruments
any adapter comparison needs: a method-neutral magnitude axis---the effective
update magnitude $\dw$ (the mean normalized output displacement the update induces,
CLoRA's $\mathbb{F}$ diagnostic), chosen over the spectral norm, which spiky
initializations inflate---and a per-method learning-rate sweep. Three claims
and a message follow. \textbf{(1) Mechanism:} forgetting is governed, to first
order, by $\dw$ rather than by adapter geometry---retention collapses onto a single
empirical curve
($r\!=\!-0.86$ pooled, $-0.92$ on the on-curve adapters), and adapter identity adds
essentially nothing once $\dw$ is controlled; the one apparent below-curve
deviation (SC-LoRA) dissolves under seed replication and an evaluation-matched
calibration control, strengthening the null. \textbf{(2) Diagnosis:} the elaborate
adapters' reported wins carry the \emph{ingredients} of a learning-rate
artifact---each peaks at a different learning rate, and the $\dw$ an adapter
produces predicts retention far better ($R^2\!=\!0.74$) than its learning rate does
($R^2\!=\!0.32$), so a per-method sweep collapses the apparent differences back
onto the shared curve. \textbf{(3) Consequence:} plain LoRA with a weight-decay
term---the simplest possible magnitude control---lands on the
adaptation--retention Pareto frontier of every elaborate geometric adapter,
matching or edging it by moving \emph{along} the shared curve---against plain LoRA
the edge is outside three-seed noise on both axes (CS-8 $81.8\!\pm\!0.2$ vs
$79.1\!\pm\!0.1$; core retention $25.9\!\pm\!0.4$ vs $23.8\!\pm\!0.6$)---and does
so with the
widest safe operating band in the study (6 of 7 learning rates versus SC-LoRA's 1
of 7). \textbf{Message:} control the size of the update first; adapter geometry
mostly sets \emph{where} on the magnitude curve a method starts, not a separate
axis of forgetting. The evidence is a
complete Llama-2-7B commonsense learning-rate grid (8 adapters $\times$ 7 rates
$=$ 56 cells, 49 clean after withholding CorDA) with three-seed error bars on the
headline operating points, a five-adapter math grid plus a faithful-recipe math
reproduction, and a complete Qwen2.5-7B commonsense replication on its own scale
(7 adapters $\times$ 7 rates $=$ 49 cells, pooled $r\!=\!-0.86$, single seed),
with a qualitative single-seed Qwen math replication ($n\!=\!56$ cells on BBH
retention, $r\!=\!-0.70$, slope $-14.4$\,pp/decade, inside the cross-domain
slope band).
Even if the still-open controls later shift the rankings, the measurement
contribution stands: the fair $\dw$ axis and the sweep-as-instrument decompose any
adapter's apparent win into a magnitude effect plus a learning-rate-tuning
artifact.
\end{abstract}

% ===========================================================================
\section{Introduction}\label{sec:intro}
% ===========================================================================
Catastrophic forgetting---the erosion of a pretrained model's general
capabilities when it is fine-tuned on a narrow task---is one of the central
obstacles to deploying adapted large language models. Parameter-efficient
fine-tuning (PEFT), and in particular LoRA~\citep{hu2021lora} and its many
descendants, was supposed to help: by restricting the update to a low-rank
correction $\Delta W = BA$, one hopes to leave the bulk of the pretrained weights
untouched. Residual forgetting persists, and the community has answered it with a
steady stream of increasingly elaborate \emph{geometric} adapters. Some initialize
the adapter from the principal or minor singular directions of the pretrained
weight (PiSSA~\citep{meng2024pissa}, MiLoRA~\citep{wang2024milora}); some project
the update orthogonally to the dominant weight subspace
(OPLoRA~\citep{xiong2025oplora}); some take a data- or activation-covariance
decomposition that freezes a ``knowledge'' subspace (CorDA~\citep{yang2024corda},
SC-LoRA~\citep{luo2025sclora}); some add an orthogonality penalty against a frozen
subspace (CLoRA~\citep{clora}); some decompose magnitude from direction
(DoRA~\citep{liu2024dora}). Each is presented as a better way to choose
\emph{where} the update goes, and each reports a win over the last.

\paragraph{Our contribution.} This paper argues those wins carry the ingredients
of a learning-rate artifact. It makes one measurement contribution and one
empirical claim, and each stands on its own. The \emph{measurement contribution}
is a fair-comparison protocol for forgetting under PEFT: a method-neutral
magnitude axis---the effective update magnitude $\dw$ (CLoRA's $\mathbb{F}$
diagnostic, Section~\ref{sec:setup}), which spectral measures
confound because spiky initializations (CorDA, DoRA, SC-LoRA) inflate them
(Section~\ref{sec:setup}, Figure~\ref{fig:axis})---and a per-method learning-rate
sweep, which exposes what single-rate comparisons hide: different adapters
transmit the same learning rate into different $\dw$, so any fixed rate flatters
whichever method happens to be tuned there (Section~\ref{sec:lr}). The
\emph{empirical claim} is what the protocol reveals: forgetting is governed by
$\dw$, essentially independently of adapter geometry. Across 49 runs and seven
adapters, retention tracks $\log\dw$ at $r\!=\!-0.86$, and adding adapter identity
to the regression buys $\Delta R^2\!=\!0.13$, almost all of it from one outlier
whose deviation dissolves under seed replication and an evaluation-matched
calibration control (Figure~\ref{fig:hero}; see Limitations). Two
consequences follow.
\emph{Diagnostically}, geometry does not change the shape of the retention curve,
only where on the curve a given learning rate lands you---so single-rate wins
dissolve under the sweep (Section~\ref{sec:lr}). \emph{Practically}, the simplest
magnitude control---plain LoRA with weight decay---lands on the
adaptation--retention frontier of every elaborate adapter we swept, with the
widest safe learning-rate band in the study (Section~\ref{sec:pareto}). All four
originally deferred controls---seeds on the headline cells, an eval-matched
calibration for the data-aware inits, a second model, and the matched-capacity
control at rank~16---have since landed and left the curve intact (see
Limitations): in the last of them, rank shifts $\dw$ and $\dw$ sets retention,
so the capacity confound resolves in the law's favor rather than unmaking the
protocol or the curve.

With the two instruments in hand, the empirical spine of the paper is three claims
and a message: \textbf{(1) Mechanism} --- forgetting is governed to first order by
$\dw$, with adapter geometry acting largely through it (Section~\ref{sec:law},
Section~\ref{sec:geometry}); \textbf{(2) Diagnosis} --- the reported
per-method wins carry the ingredients of a learning-rate artifact that a
per-method sweep dissolves (Section~\ref{sec:lr}); \textbf{(3) Consequence} ---
plain LoRA{+}weight-decay lands on the adaptation--retention Pareto frontier of
every elaborate geometric adapter, as a corollary of the law
(Section~\ref{sec:pareto}); and the \textbf{message} --- control the size of the
update first; geometry is a fingerprint, not a separate lever. For coverage:
across the seven of eight adapters we can assess
(LoRA, LoRA-Null, LoRA{+}wd, DoRA, MiLoRA, CLoRA, SC-LoRA), six lie on a single
$\dw$ curve; SC-LoRA's seed-42 point sits below it, but seeds 43/44 and an
eval-matched calibration control both return it to the curve---a resolved
seed-and-calibration confound, not a geometric penalty
(see Limitations)---and CorDA is
withheld pending a calibration-fairness fix (see Limitations).

\paragraph{Relation to concurrent work.} Independently and from a different
codebase, \citet{lee2026lr} show that the peak \emph{adaptation} of nine LoRA
variants converges once each is tuned at its own learning rate. We establish the
retention-side counterpart: forgetting is governed by the magnitude that the
learning rate produces, and geometry adds essentially nothing beyond that magnitude
among the on-curve adapters (no subspace-alignment metric adds more than
$\Delta R^2\!\approx\!0.02$---increments occasionally significant, always
practically negligible---and the strongest single candidate axis, input-principal
alignment $e^{\rm in}_{\rm top}$, adds $0.0002$, n.s.;
Section~\ref{sec:geometry})---corroborating their adaptation finding while extending
it to the axis that the forgetting-mitigation literature actually cares about.

\paragraph{Scope, stated once.} The Llama-2-7B commonsense evidence is complete:
the full $8\times 7$ learning-rate grid (49 clean cells; CorDA's 7 cells withheld,
the Limitations section), with seeds 43/44 added on the headline operating
points (three-seed triples on all seven assessed adapters). The math arm
covers five adapters on the sweep recipe ($n\!=\!35$) plus a faithful-recipe
reproduction block, and the Qwen2.5-7B arm replicates commonsense in full
(7 adapters $\times$ 7 rates $=$ 49 cells, pooled $r\!=\!-0.86$; single seed);
its math arm replicates the law qualitatively ($n\!=\!56$ single-seed cells on
BBH retention, $r\!=\!-0.70$, slope $-14.4$\,pp/decade inside the cross-domain
band; Section~\ref{sec:law}).
The Limitations section states the remaining gaps explicitly---the geometry
battery is single-seed, ranks are unmatched across arms (though a rank-16
control now closes the capacity confound), CorDA is withheld, and the raw
magnitude correlation is near-tautological on its own (we rest the claim on the
residual battery instead).

% ===========================================================================
\section{Related Work and Positioning}\label{sec:related}
% ===========================================================================
\paragraph{The adapter zoo, organized by basis.} The forgetting-mitigation
literature is best organized not by surface mechanism but by the single
load-bearing axis on which the methods actually differ: the \emph{basis} in which
they reason about which directions to protect.
\emph{(i) Static weight-SVD basis.} PiSSA~\citep{meng2024pissa} initializes from
the top singular triples of $W_0$; MiLoRA~\citep{wang2024milora} from the bottom
(minor) triples; OPLoRA~\citep{xiong2025oplora} projects the update into the
orthogonal complement of the top-$k$ singular subspace, provably preserving the
dominant singular triples. \emph{(ii) Data / activation-covariance basis.}
CorDA~\citep{yang2024corda} takes the SVD of $W_0\,\mathrm{Cov}_{\text{input}}$
over a calibration set and freezes the components most responsive to that data;
SC-LoRA~\citep{luo2025sclora} constructs an output subspace from a balance of task
and preservation covariances; LoRA-Null~\citep{tang2025loranull} initializes the
adapter in the null space of the input-activation covariance, so the initial
update avoids the directions the calibration data exercises;
CorDA++~\citep{yang2025cordapp} adds dynamic covariance selection and per-layer
rank allocation. \emph{(iii) Auxiliary loss /
other.} CLoRA~\citep{clora} penalizes the overlap of the adapter with a pre-defined
(e.g.\ random-orthonormal) subspace; DoRA~\citep{liu2024dora} decomposes each
column of the update into magnitude and direction. What every one of these shares
is a commitment to \emph{geometry}: the premise that \emph{where} the update lands
is the lever that controls forgetting. Our results say the lever is instead
\emph{how big} the update is.

\paragraph{Magnitude and forgetting, before and outside the adapter zoo.} The
idea that forgetting is controlled by how far the weights move is not new; what is
new is showing it is the \emph{whole} story once the measurement is fair. In the
pre-LLM transfer-learning literature, EWC~\citep{kirkpatrick2017ewc} penalizes
Fisher-weighted deviation from the pretrained weights and
L$^2$-SP~\citep{li2018l2sp} recommends a plain $L^2$ penalty toward the starting
point---magnitude regularization with and without a geometric weighting. For LLM
fine-tuning, \citet{kalajdzievski2024scaling} fits scaling laws in which
forgetting grows as a shifted power law in the number of parameters tuned and the
number of update steps---coarse proxies for update size---and
\citet{biderman2024lora} show that LoRA forgets less than full fine-tuning while
learning less, explicitly framing the two as coupled. Even inside the adapter zoo,
two recent methods report forgetting that moves with update size, in passing:
MiLoRA's own WikiText forgetting loss orders PiSSA${>}$LoRA${>}$MiLoRA (their
Table~8~\citep{wang2024milora}, using the CE-to-base metric
of~\citealp{kalajdzievski2024scaling}), tracking the update amplification they
tabulate, and LoRA-Null's Table~4b shows CorDA's retention collapsing
($89\%{\to}73\%$) as its rank---hence its update capacity---grows
\citep{tang2025loranull}. Both are consistent with the magnitude law, but each is
reported for one method's own design. None of these works measures
$\dw$ directly across methods, compares adapter geometries on a common magnitude
axis, or sweeps the learning rate per method---so none can ask whether geometry adds
anything \emph{beyond} magnitude, which is our question.

One point of apparent tension deserves a direct answer.
\citet{biderman2024lora} also tested weight decay as a forgetting mitigation for
\emph{full fine-tuning} and found it learned and forgot like the unregularized
baseline---seemingly the opposite of our Claim~3. The two results are consistent
under the magnitude law: they applied small coefficients ($5\times10^{-5}$,
$10^{-4}$) to all seven billion parameters at a single previously-tuned learning
rate, a setting in which the penalty barely moves $\dw$; we apply weight decay
$0.3$ to the adapter matrices alone under a per-method rate sweep, which
demonstrably walks the model down the retention curve
(Section~\ref{sec:pareto}). Indeed, their single-rate design is itself an
instance of the comparison our Claim~2 is about: without a per-method sweep, a
regularizer's effect on the adaptation--retention trade-off cannot be separated
from its effect on the effective learning rate.

\paragraph{Why magnitude is overlooked in the adapter literature.} Weight decay is
the oldest, simplest knob in deep learning, and it acts directly on the \emph{size}
of the update rather than its direction. Precisely because it carries no geometric
story, it is almost never the protagonist of an adapter paper. Within the
PEFT-adapter literature, the prior work that most explicitly connects update
magnitude to forgetting is CLoRA, whose penalty can be read as an indirect
magnitude control. Our contribution is therefore \emph{not} the discovery that
magnitude matters. It is (i) that magnitude is the \emph{method-free} governing
variable; (ii) that once $\dw$ and method identity are controlled, subspace
geometry adds essentially nothing measurable (no alignment metric adds more than
$\Delta R^2\!\approx\!0.02$, increments occasionally significant but practically
negligible, and the strongest single candidate axis, input-principal alignment,
adds $0.0002$, n.s.; Section~\ref{sec:geometry})---a
claim no single-method paper such as CLoRA, no
regularization study such as \citet{biderman2024lora} or
\citet{kalajdzievski2024scaling} (neither of which places competing geometries on
a controlled-$\dw$ axis), and no adaptation-only study such as \citet{lee2026lr}
(who link the differing optimal learning rates to the largest Hessian eigenvalue
but do not study retention), is positioned to make; and (iii) that the reported
per-method wins carry the ingredients of a learning-rate artifact that a
per-method sweep dissolves (Section~\ref{sec:lr}).

% ===========================================================================
\section{Setup and Measurement}\label{sec:setup}
% ===========================================================================
\paragraph{Models and domains.} Our primary, complete evidence is on
\textbf{Llama-2-7B}; a single-seed replication on \textbf{Qwen2.5-7B} covers
commonsense in full, with its math arm a qualitative, directional replication
(Section~\ref{sec:law}). We fine-tune
on two domains: \emph{commonsense} (the eight-task LLM-Adapters commonsense
suite~\citep{hu2023llmadapters}) and \emph{math} (trained on MetaMathQA, evaluated
on GSM8K).

\paragraph{Why a common harness is a prerequisite, not a convenience.} A fair
cross-method comparison of \emph{forgetting} cannot be assembled from the published
numbers, because the adapter papers do not measure retention on a common axis.
Among the competitors, \emph{only} CLoRA~\citep{clora} reports our retention
benchmarks (BBH and MMLU-Pro); the data-aware inits report closed-book factoid-QA
exact-match (CorDA, SC-LoRA, and LoRA-Null on TriviaQA/NQ/WebQS), MiLoRA reports
only a WikiText loss proxy, and PiSSA and DoRA report no forgetting metric at all.
Their ``retention'' numbers therefore cannot be placed on a single axis. Evaluating
every adapter ourselves under one identical harness is thus not a matter of
convenience but a precondition for any fair forgetting comparison---and is itself
part of the measurement contribution.

\paragraph{The adapters, as one trainer.} A key design decision makes the
comparison fair: every structured adapter is implemented as ``LoRA with a different
initialization'' loaded into a single shared PEFT LoRA trainer, so that the only
difference between arms is the init routine---extracted as $(A_{\text{init}},
B_{\text{init}}, W_{\text{residual}})$---and, for CLoRA alone, one added loss term.
We study eight arms: plain \textbf{LoRA} (r16), \textbf{LoRA{+}wd} (r32, weight
decay 0.3 on the adapter matrices only), \textbf{DoRA} (r16), \textbf{CorDA} (r16,
KPA retention mode, nq\_open calibration), \textbf{MiLoRA} (r32), \textbf{SC-LoRA}
(r32, $\beta{=}0.5$), \textbf{LoRA-Null} (r16), and \textbf{CLoRA} ($k{=}1024$).
LoRA-Null~\citep{tang2025loranull} initializes the adapter from the null space of
the input-activation covariance---calibrated, like CorDA and SC-LoRA, on
nq\_open---so that the initial update avoids the directions the calibration data
exercises. Two members of the static weight-SVD family are positioned in
Section~\ref{sec:related} but not swept: PiSSA, whose principal-triple
initialization targets adaptation speed rather than retention (MiLoRA is that
family's retention-oriented member and represents it here), and OPLoRA, whose
projection protects exactly the weight-SVD subspace that our companion
direction diagnostics find uninformative for retention
(Section~\ref{sec:law}).
Before claiming any method ``fails,'' we audited each port against its reference
implementation; all ports are faithful, and the residual-init methods (MiLoRA,
SC-LoRA) pass a 0-step self-check that a $\Delta W = 0$ update recovers base
retention. CorDA did \emph{not} pass a clean self-check under the current
calibration and is withheld (see Limitations). Ranks are \emph{not}
matched across arms: LoRA{+}wd's $r{=}32$ matches the three elaborate $r{=}32$
adapters it is most directly compared against (MiLoRA, SC-LoRA, and CLoRA's
$r{=}32$ configuration) but doubles plain LoRA's and DoRA's $r{=}16$---a
mismatch a rank-16 LoRA{+}wd control now closes, reproducing the operating
point at plain LoRA's parameter count (see Limitations)---and one further asymmetry is
inherent to the residual construction: the plain arms use scaling $\alpha\!=\!2r$,
whereas the residual-init arms (CorDA, MiLoRA, SC-LoRA, LoRA-Null) are forced to
$\alpha\!=\!r$ by the rank-$2r$ residual conversion, so a fixed nominal learning
rate maps to a different update growth per family. The $\dw$ reparametrization
absorbs this: it shifts a method \emph{along} the learning-rate grid, not off the
retention curve.

\paragraph{Scale validation.} Our accuracy scale reproduces the canonical
Llama-2-7B LoRA anchor. Run at the canonical LLM-Adapters commonsense learning rate
($3\text{e-}4$)~\citep{hu2023llmadapters}, our plain LoRA scores BoolQ $71.1$
(three-seed mean $70.7$) against the canonical $69.8$ (DoRA's reproduction under
the LLM-Adapters
recipe~\citep{liu2024dora}), and its eight-task CS-8 accuracy of $79.1$ sits
between the two published Llama-2-7B LoRA baselines reported under that
recipe---DoRA's $77.6$ and CLoRA's $79.9$~\citep{clora}. Our accuracies are
therefore on the established scale for this setup, so the retention numbers we
report are trustworthy in absolute terms.

\paragraph{Learning-rate sweep.} We sweep seven learning rates per method,
$\{2\text{e-}5, 5\text{e-}5, 1\text{e-}4, 2\text{e-}4, 3\text{e-}4, 5\text{e-}4,
1\text{e-}3\}$. The two highest rates originally in the grid (2e-3, 5e-3) diverged
to NaN weights on every method and are dropped; the forgetting regime is already
reached at 1e-3. The per-method sweep is the paper's central instrument
(Section~\ref{sec:lr}).

\paragraph{Retention and adaptation metrics.} \emph{Retention (core)} is the mean
of BBH (answer-only, 3-shot) and MMLU-Pro (5-shot CoT); the no-fine-tuning base
ceiling is $33.10$ and $18.96$ respectively, for a core ceiling of $\mathbf{26.0}$
(an in-registry no-fine-tuning replicate reproduces it at $25.89$).
\emph{Retention (broad)} adds MMLU, ARC-Challenge, and TruthfulQA; their
no-fine-tuning ceilings are $40.88$, $44.80$, and $38.85$ respectively, for a
broad ceiling of $\mathbf{35.26}$, so broad retention is calibrated on the same
footing as the core metric. \emph{Adaptation} is eight-task
commonsense accuracy (CS-8) or GSM8K exact-match (math).

\paragraph{The magnitude axis $\dw$.} Our governing variable is the
\textbf{effective update magnitude}: the mean normalized output displacement the
update induces on real token traffic, which is exactly CLoRA's $\mathbb{F}$
forgetting diagnostic~\citep{clora} (their Eq.~3). Let $\mathcal{L}$ be
the set of adapted weight matrices, let $\Delta W_\ell$ be module $\ell$'s total
weight update (with any initialization residual included, so that a $0$-step run
gives $\Delta W_\ell = 0$), and let $x_{\ell,t}$ be the input activation of token
$t$ at module $\ell$, captured by forward hooks while running a fixed batch of 100
held-out commonsense evaluation prompts---the same probe set for every run in both
domains, so the measure is comparable across all cells. With $N_\ell$ the
number of tokens reaching module $\ell$, we define
\begin{equation}\label{eq:fdelta}
  \dw \;=\; \frac{1}{\sum_{\ell\in\mathcal{L}} N_\ell}
  \sum_{\ell\in\mathcal{L}} \sum_{t=1}^{N_\ell}
  \frac{\lVert \Delta W_\ell\, x_{\ell,t} \rVert_2}{\lVert x_{\ell,t} \rVert_2}.
\end{equation}
Each term is the normalized output displacement the update causes on real token
traffic; for isotropic inputs its square concentrates on
$\lVert\Delta W_\ell\rVert_F^2 / d_{\text{in}}$ in the high-dimensional limit (as a
ratio of expectations, where $\lVert x\rVert_2^2$ concentrates), so it behaves like a
per-dimension, token-weighted Frobenius norm while being defined as an effective
\emph{output displacement} rather than a raw matrix norm. We aggregate it
token-weighted over all module--token pairs rather than averaging per matrix, so our
$\dw$ values live on the same normalized scale as---and are directly comparable
to---CLoRA's Table~4 (their LoRA $0.79$, LoRA-L2 $0.29$, $k$2048 $0.14$; our spectral
fields $\smax$/$\bar\sigma$ correspond instead to their $\lVert\Delta W\rVert$
column);
values range $\sim\!0.05$--$3.7$ on the clean sweep (estimator details and
registry-field mapping in Appendix~\ref{app:repro}).
We adopt this measure over the maximum spectral norm $\smax$ because $\smax$ is
method-confounded: spiky inits (CorDA, DoRA, SC-LoRA) inflate it, which would
mis-rank them. Figure~\ref{fig:axis} shows that among three candidate magnitude
measures ($\dw$, mean spectral norm, and $\smax$), $\dw$ gives the tightest, most
method-neutral fit---it explains $R^2\!=\!0.74$ of the variance in retention
versus $0.36$ for the mean spectral norm and $0.33$ for $\smax$---so it is the fair
$x$-axis for the whole paper. All pooled figures aggregate seven series---LoRA, LoRA-Null,
LoRA{+}wd, DoRA, MiLoRA, CLoRA, SC-LoRA---for
$n\!=\!49\!=\!7\text{ series}\times7\text{ LR}$; CorDA is withheld
(see Limitations).

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig1_magnitude_law.png}
  \caption{\textbf{Choosing a fair magnitude axis.} Retention versus three
  candidate magnitude measures (log $x$-axis), pooled over all adapters.
  The effective update magnitude $\dw$ (highlighted panel) gives the tightest fit
  ($r\!=\!-0.86$, $R^2\!=\!0.74$); $\smax$ is loosest and method-confounded because
  spiky inits inflate it. We adopt $\dw$ as the axis for the entire study.}
  \label{fig:axis}
\end{figure*}

% ===========================================================================
\section{The Magnitude Law}\label{sec:law}
% ===========================================================================
Our central result is an empirical regularity we call the magnitude law (``law''
in the descriptive, not the axiomatic, sense): retention is a tight, monotone,
\emph{method-independent} function of $\dw$. Figure~\ref{fig:hero} is the central
exhibit: plotting core retention against $\dw$ (log scale) for all adapters,
points from six of the seven series interleave along one downward curve (the sole
below-curve series on this seed, SC-LoRA, is ringed; its deviation resolves as a
seed-and-calibration artifact; see Limitations).

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig0_hero.png}
  \caption{\textbf{The magnitude law.} Core retention (mean of BBH and MMLU-Pro)
  versus $\dw$ (log scale), Llama-2-7B commonsense, seed 42, seven adapter series.
  Points from six of the seven series interleave along one curve. Black line:
  pooled fit on the six on-curve adapters with a 95\% bootstrap CI
  ($r\!=\!-0.92$, $R^2\!=\!0.84$, slope $-10.0$\,pp/decade, $n\!=\!42$);
  all-adapters pooled fit $r\!=\!-0.86$, slope $-14.8$\,pp/decade ($n\!=\!49$).
  SC-LoRA is ringed: its seed-42 series sits below the curve, a deviation that
  seeds 43/44 and an eval-matched calibration control both resolve
  (see Limitations). The star marks the LoRA{+}wd best-adapt point
  (Table~\ref{tab:cs}): the practical punchline of Section~\ref{sec:pareto} is
  that the simplest magnitude control sits at the low-$\dw$, high-retention end
  of this same curve. Green dotted line: base ceiling (26.0).}
  \label{fig:hero}
\end{figure}

\paragraph{The correlations.} On Llama-2 commonsense, retention is a tight,
monotone, method-independent function of $\dw$ in which the methods
\emph{interleave}: the pooled correlation with $\log_{10}\dw$ is $r\!=\!-0.86$
($R^2\!=\!0.74$, slope $-14.8$\,pp/decade, $n\!=\!49$,
$p\!=\!3\!\times\!10^{-15}$), the six on-curve adapters tighten it to
$r\!=\!-0.92$ ($R^2\!=\!0.84$, slope $-10.0$\,pp/decade, $n\!=\!42$), and
\emph{every adapter individually} traces the same line, with within-method
correlations from $-0.86$ to $-0.97$ (plain LoRA $-0.95$, LoRA-Null $-0.86$,
LoRA{+}wd $-0.89$, DoRA $-0.97$, MiLoRA $-0.94$, CLoRA $-0.90$, SC-LoRA $-0.97$)
rather than method-specific sub-curves.

\paragraph{The relationship is monotone and saturating, not a linear artifact.} A
rank-based Spearman correlation is $\rho\!=\!-0.90$ ($p\!=\!3.5\times10^{-18}$),
slightly stronger than the Pearson value, so the monotonicity does not depend on the
linear parametrization. The functional form is in fact a saturating one: a
hockey-stick fit (a floored linear decline) beats both a straight line and a
quadratic on AIC \emph{and} on leave-one-out cross-validation, with a knee at
$\dw\!\approx\!0.36$---almost exactly LoRA{+}wd's operating point
(Section~\ref{sec:pareto})---and a fitted asymptote ($26.8$) essentially at the
base ceiling ($26.0$);
the quadratic is the worst out-of-sample because its upturn at small $\dw$ is
unphysical. Below the ceiling (retention $<25$, where the curve is not floored) the
slope steepens to $-21$\,pp/decade (Tobit $-22$ accounting for censoring), so the
pooled $-14.8$ if anything \emph{understates} the true cost of magnitude. Finally,
the partial correlation of retention with $\log\dw$ \emph{controlling for method
identity} is $-0.87$, and a method-label permutation test rejects the null at
$p\!<\!5\times10^{-5}$: the association is between magnitude and retention, not an
artifact of which methods happen to sit where.

\paragraph{Second-model evidence: a full two-domain replication (commonsense
complete; math qualitative).} To test whether
the law is a Llama idiosyncrasy we replicated the design on Qwen2.5-7B (single
seed, s42). The \emph{commonsense} arm is a complete second-architecture
replication: the same 7 adapters $\times$ 7 learning rates ($n\!=\!49$ assessed
cells) give a pooled core-retention correlation of $r\!=\!-0.86$
($R^2\!=\!0.74$, $p\!=\!3.7\!\times\!10^{-15}$; broad metric $r\!=\!-0.94$)---%
numerically indistinguishable from Llama-2's $-0.86$. The slope is steeper
($-32.0$\,pp/decade vs Llama's $-14.8$), which reflects Qwen's retention scale
(a different base ceiling, not yet measured for Qwen; see Limitations), not a
different law; we therefore never merge the
two models into one fit. Within-method correlations run $-0.73$ to $-0.91$ for
six of the seven adapters; LoRA{+}wd alone is flat ($r\!=\!-0.17$) \emph{by
construction}---weight decay caps $\dw$, so its seven points do not span the
axis, the same shallow signature it shows on Llama. The \emph{math} arm
(MetaMathQA$\rightarrow$GSM8K) now replicates the law's direction. On the
completed single-seed grid, $n\!=\!56$ assessed cells give a BBH-retention fit
of $r\!=\!-0.70$ with slope $-14.4$\,pp/decade over $\dw\ 0.029$--$0.90$
($\sim$1.5 decades); two diverged cells (a plain-LoRA lr5e-4 run at
$\dw\!=\!20.9$ with all-zero evals and a LoRA{+}wd lr1e-3 sibling) are excluded
under the divergence convention of Section~\ref{sec:setup} and disclosed here.
The slope sits inside the cross-domain band ($-12.7$ to $-18.9$; Llama's
BBH-matched $-14.3$ and pooled $-14.8$), the within-method fits (computed over
all seeds) are negative for every unconstrained adapter (LoRA $-0.84$, SC-LoRA
$-0.82$, CLoRA $-0.79$, DoRA $-0.70$, MiLoRA $-0.65$, LoRA-Null $-0.63$), and
LoRA{+}wd alone is flat ($+0.09$) \emph{by construction}---the same signature
as both commonsense arms. An all-seeds variant that adds the seven s43/s44
replicate cells agrees ($n\!=\!63$, $r\!=\!-0.66$, $-13.3$\,pp/decade). We
state the strength honestly: this is a \emph{qualitative, directional}
replication---single-seed, BBH-only retention, and no calibrated Qwen base
ceiling---not a quantitative law match. Qwen math retention is reported on BBH
only; MMLU-Pro is excluded by convention, having been unreliable on the early
Qwen-math rows. Both Qwen arms are single-seed (s42) apart from those seven
math replicate cells; we replicate the law on commonsense in full and on math
directionally, and draw no Qwen method ranking.

\paragraph{Both domains.} The law is not a commonsense idiosyncrasy on Llama
either. On the math sweep (MetaMathQA$\rightarrow$GSM8K, same 7-rate design)
five adapters are complete---LoRA, LoRA{+}wd, DoRA, CLoRA, and MiLoRA, 7 rates
each---and the pooled correlation is $r\!=\!-0.95$ ($R^2\!=\!0.90$, slope
$-8.9$\,pp/decade, $n\!=\!35$, $p\!=\!8\!\times\!10^{-18}$); the original
three-adapter subset gives $r\!=\!-0.97$ (slope $-9.3$, $n\!=\!21$). An
independent faithful-recipe math block (each method under its published math
recipe rather than our sweep recipe; Section~\ref{sec:pareto}) reproduces the
same law on BBH at $r\!=\!-0.89$, slope $-14.3$\,pp/decade ($n\!=\!50$
non-diverged cells)---so math is a genuine second domain, not a sparse
afterthought.

\paragraph{External corroboration from a competitor's own data.} One competitor,
CLoRA~\citep{clora}, runs our exact setup---Llama-2-7B, the LLM-Adapters
commonsense suite, MetaMathQA$\rightarrow$GSM8K---and, uniquely among the
competitors, reports forgetting on our exact axis (BBH and MMLU-Pro via
\texttt{lm-eval}), so its published numbers independently triangulate the magnitude
law. Three facts in its tables point the same way as ours. \emph{(i)} Plain LoRA
forgets on both benchmarks relative to base (BBH $34.91\!\to\!26.69$, MMLU-Pro
$18.56\!\to\!14.46$), in the same direction as our own retention degradation as
$\dw$ grows. \emph{(ii)} CLoRA reports an $L^2$-regularization
baseline (``LoRA-L2'': BBH $32.93$, MMLU-Pro $16.59$)---the closest published
analog to our LoRA{+}wd---which recovers much of LoRA's lost retention, exactly as
a magnitude control should. \emph{(iii)} Its forgetting proxy
$\mathbb{F}=\lVert\Delta W x\rVert/\lVert x\rVert$ is the \emph{same} metric our
$\dw$ implements (Section~\ref{sec:setup}), so its Table~4 is a genuine external
replication of the law rather than a mere analogy: across its ten fine-tuned
adapter rows (reproduced in full in Table~\ref{tab:clora}; the no-fine-tuning
Base row is excluded as a reference point, the same rule as our own fits),
retention (BBH) falls with $\log_{10}\mathbb{F}$ at $r\!=\!-0.98$ and pooled slope
$-14.65$\,pp/decade, on an entirely independent dataset (LoRA $\mathbb{F}\!=\!0.79$,
rsLoRA $0.92$, LoRA-L2 $0.29$, CLoRA's $k$-series smaller still). We read the
pooled slope's proximity to our own $-14.8$ not as a point match---it is a
fortuitous blend of a $-12.7$\,pp/decade baseline family and a steeper
$-18.9$\,pp/decade $k$-series (Table~\ref{tab:clora})---but as parallel slopes
within one band: ours ($-14.3$ BBH-matched, $-14.8$ pooled) and theirs ($-12.7$
to $-18.9$), the same sign and order of magnitude. Crucially, we do \emph{not} line these
numbers up one-to-one: CLoRA's base BBH ($34.91$) exceeds our answer-only base
($33.10$), a harness-configuration gap, so we read CLoRA as independent
triangulation---relative degradation from each paper's own base points the same
way---rather than as a shared coordinate system (Appendix~\ref{app:clora}).

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig_cross_literature}
  \caption{\textbf{The same law appears in a competitor's own published table.}
  Retention (BBH) versus $\dw$ on a log axis, plotted on a \emph{BBH-to-BBH} basis
  so the two datasets are directly comparable. Both point clouds fall with $\dw$ in
  the same direction: CLoRA's Table~4 (all ten fine-tuned rows, Table~\ref{tab:clora};
  their Base reference row excluded by construction) at $r\!=\!-0.98$ and
  ours at $r\!=\!-0.79$, and the association is robust---dropping any single CLoRA
  row leaves $r\!\le\!-0.95$. The two fits are drawn separately (\emph{parallel
  slopes, not one shared line}): our BBH-to-BBH slope is $-14.3$\,pp/decade, and
  CLoRA's pooled slope is $-14.65$\,pp/decade, itself a blend of a
  $-12.7$\,pp/decade baseline family and a steeper $-18.9$\,pp/decade $k$-series.
  Absolute $\dw$ levels are comparable where the recipes match---CLoRA's LoRA
  baseline sits at $\mathbb{F}\!=\!0.79$, essentially our own LoRA magnitude---and
  only their null-space-\emph{constrained} $k$-series runs lower. CLoRA's $k$1024 and
  $k$2048 configurations exceed their own base BBH ($34.91$), i.e.\ they \emph{gain}
  out-of-domain accuracy by shrinking the update, which is the magnitude law read
  from the other end. Two independent datasets and pipelines, one direction and one
  slope band (Appendix~\ref{app:clora}).}
  \label{fig:crosslit}
\end{figure}

\paragraph{The law acts per capability.} Decomposing retention into its constituent
benchmarks (Figure~\ref{fig:perbench}, Appendix~\ref{app:figs}), every capability
degrades monotonically with $\dw$ except one: broad factual knowledge dies fastest
(MMLU at $-23.4$\,pp/decade, $r\!=\!-0.93$; MMLU-Pro $-15.2$, ARC-Challenge
$-14.9$, BBH $-14.3$), while TruthfulQA is essentially flat ($-0.46$\,pp/decade,
$r\!=\!-0.10$, n.s.). The base ceiling confirms that the TruthfulQA immunity is
real, not a floor artifact: against its $38.85$ no-fine-tuning ceiling, the 49
fine-tuned cells span $31.4$--$39.5$ (mean $35.7$, with $1$ of $49$ above
base)---fine-tuning imposes a small \emph{constant} offset
($\approx\!-3$\,pp), independent of magnitude, rather than a slope. This
texture---one law with a per-capability
slope---is evidence that the phenomenon is not an artifact of a single metric.

% ===========================================================================
\section{Geometry Is a Second-Order Fingerprint, Not a Separate Lever}
\label{sec:geometry}\label{sec:ancova}
% ===========================================================================
The central negative result of this paper is that adapter \emph{geometry}---the
subspace an initialization or penalty steers the update into---adds essentially
nothing to retention once the update magnitude $\dw$ is held fixed. The strongest
single candidate axis, input-principal alignment ($e^{\rm in}_{\rm top}$), buys
$\Delta R^2\!=\!0.0002$ ($p\!=\!0.60$) beyond $\dw$ and
method identity; across the full five-metric alignment battery no term buys more
than $\Delta R^2\!\approx\!0.01$ on the 219-adapter commonsense set (or
$\approx\!0.02$ on on-curve subsets)---increments occasionally statistically
significant, always practically negligible---and the headline candidate
``geometric axis,'' concentration of
the update in the base weight's principal singular directions, collapses or
\emph{flips sign}
the moment its two principal-space-init outliers are removed
(Section~\ref{sec:fingerprint}). We report this as \emph{rigor, not retraction}: we
looked hard for a second, geometric axis of forgetting, testing it directly on
all 320 saved adapters, and did not find one. What survives is constructive---the
same measurements that rule geometry out as a \emph{lever} make it an excellent
\emph{fingerprint}, one that recovers each method's advertised design from its
trained weights and explains the study's two below-curve outliers mechanistically.

This is the result that separates our claim from everything prior. The raw
correlation ``a bigger update forgets more'' is near-tautological, so we do not rest
the claim on it; the non-trivial, falsifiable statement is that \emph{adapter
identity adds essentially nothing beyond $\dw$}. No prior method comparison is
positioned to make it: CLoRA argues from a single method, and the concurrent
adaptation study of \citet{lee2026lr} measures task accuracy, not retention. We
test it three ways.

\emph{Intercepts (ANCOVA).} A pooled log-linear fit gives $R^2\!=\!0.74$; adding
per-method intercepts raises it to $R^2\!=\!0.87$
(Figure~\ref{fig:fairness})---an improvement that is significant overall
($F(6,41)\!=\!7.1$, $p\!<\!0.001$) but driven almost entirely by a single method.
The per-method residuals from the pooled spline curve are $+0.99$\,pp (LoRA),
$+0.60$ (LoRA-Null), $+0.06$ (LoRA{+}wd), $+1.04$ (MiLoRA), $+0.09$ (CLoRA), and
$+1.37$ (DoRA)---all statistically indistinguishable from zero---and $-4.15$\,pp
for SC-LoRA ($p\!=\!0.006$), the sole significant deviator on this seed.
Restricted to the six
on-curve adapters, the intercepts are jointly non-significant
($F(5,35)\!=\!1.79$, $p\!=\!0.14$). The full-pool significance is specific to
seed~42: at SC-LoRA's operating point, seeds 43 and 44 return its retention to
the curve ($25.6$/$25.8$ vs $22.5$; three-seed $24.6\!\pm\!1.8$, tracking the
lower $\dw$ those runs land at),
and an eval-matched calibration control lifts it to $26.5$--$27.0$, at or above
the $26.0$ base ceiling
(see Limitations), so the deviation that drives
$F(6,41)\!=\!7.1$ is a seed-and-calibration artifact rather than a stable
geometric penalty.

\emph{Slopes.} Geometry could in principle bend the curve rather than shift it,
so we also test per-method slopes. Among the six on-curve adapters the slopes are
statistically indistinguishable ($F(5,30)\!=\!0.28$, $p\!=\!0.92$; per-method
slopes $-8.1$ to $-11.7$\,pp/decade). Over all seven series the slope interaction
is significant ($F(6,35)\!=\!9.3$, $p\!<\!0.001$), again driven entirely by
SC-LoRA (slope $-26.0$): the one seed-42 deviator differs in both level and
slope, and the other six share one line. (Like the intercept test, this
full-pool statistic is single-seed; the SC-LoRA cell that drives it regresses to
the curve on seeds 43/44.)

\emph{Prediction (leave-one-method-out).} Finally, we ask whether the pooled law
\emph{predicts} an adapter it has never seen. Fitting the law on six series and
predicting the seventh yields held-out RMSEs of $1.7$--$3.6$\,pp (mean $2.5$)
across the six on-curve adapters---about the \emph{in-sample} accuracy of a
method-aware model with seven free intercepts ($2.2$\,pp). Held-out SC-LoRA is
mispredicted by $-7.4$\,pp on average (RMSE $9.1$) on seed~42, consistent with
its below-curve position on that seed (resolved by seeds and calibration,
the Limitations section).

\emph{An independent forgetting metric agrees.} The three tests above all live on
our retention benchmarks; a fourth check uses a wholly different axis. We compute
each adapter's cross-entropy-to-base---the soft CE between the fine-tuned and base
next-token distributions on WikiText-103, the forgetting proxy MiLoRA report in
their Table~8 following \citet{kalajdzievski2024scaling}. These CE runs are on the
faithful-recipe \emph{math} arm (MetaMathQA, $r\!=\!64$, $\alpha\!=\!128$;
Section~\ref{sec:pareto}), not the commonsense sweep, which is what makes the
rank-matched comparison below possible. It reproduces MiLoRA's
published ordering (our plain LoRA $3.57$ against their $3.24$; PiSSA $6.31$ against
their $6.07$; PiSSA${>}$LoRA), and it rises monotonically with $\dw$ (Spearman
$\rho\!=\!0.94$ on the six-row comparison set; $\rho\!=\!0.98$ over all 49
CE-profiled cells). The decisive comparison is that at a \emph{matched} learning rate
($3\text{e-}4$) and rank ($r\!=\!64$ for both; MiLoRA's stored rank-$128$ adapter
is the rank-$2r$ residual-save format), MiLoRA's CE-to-base ($3.66$) is
indistinguishable from
plain LoRA's ($3.57$): MiLoRA does not forget less once magnitude is held fixed, and
its reported advantage traces to a lower-magnitude operating point rather than to the
minor-singular subspace---the magnitude law confirmed on a metric we did not design.

\paragraph{No residual geometric axis survives the stress test.} A natural worry is
that some \emph{direction} of the update---not just its size---carries retention. We
tested the strongest candidate: the concentration of the update in the base weight's
top (principal) singular directions, via an SVD-alignment battery over all 320 saved
adapters (Section~\ref{sec:fingerprint}). The preliminary partial correlations
looked promising ($-0.48$ to $-0.58$ between the three principal-alignment
metrics and retention once
$\dw$ is regressed out), but they are carried \emph{entirely} by two
principal-space-init methods (PiSSA and SC-LoRA): drop those two and two of the
three metrics
\emph{flip sign} (amp$_{\rm top}$ $+0.31$, $e^{\rm in}_{\rm top}$ $+0.41$) while
the third, $e_{\rm top}$, stays weakly negative at $-0.17$; among the
on-curve adapters alone all three are near zero ($-0.03$ to $-0.12$, n.s.).
Added to a model that already contains $\dw$ and method identity, the strongest
single alignment term, $e^{\rm in}_{\rm top}$, buys $\Delta R^2\!=\!0.0002$
($p\!=\!0.60$); the other four battery metrics ($e_{\rm top}$, amp$_{\rm top}$,
$e_{\rm bot}$, $e^{\rm in}_{\rm bot}$) buy $\Delta R^2\!=\!0.002$--$0.010$ on the
full 219-adapter commonsense set---each nominally significant ($e_{\rm top}$ at
$p\!<\!0.001$, the rest at $p\!<\!0.05$), none material---and on the
on-curve-six subset all five terms reach nominal significance while staying
small ($\Delta R^2\!=\!0.003$--$0.019$, $p\!<\!0.03$); no metric exceeds
$\Delta R^2\!\approx\!0.02$ on any subset we test. The independent
CE-to-base check agrees: MiLoRA (minor-space init) forgets like plain LoRA at matched
magnitude (above). No alignment metric carries a practically meaningful second,
geometric axis of forgetting once magnitude is controlled.

\paragraph{Resolving the tension: geometry sets \emph{where} on the axis, not a
second axis.} Two facts sit side by side---geometry adds nothing measurable among
magnitude-matched adapters, yet the two most geometrically distinctive methods
(PiSSA, and on seed 42 SC-LoRA) are exactly the ones that show up as
outliers---and they are not in
conflict. Geometry acts by setting \emph{where on the magnitude axis a method
starts}, not by adding a dimension orthogonal to it. A principal-space
initialization begins training aligned with the directions the loss gradient most
excites, so at a fixed learning rate it accumulates a \emph{larger, more
mis-allocated} update: it forgets more because it moves further, not because
``principal'' is intrinsically fragile. Among adapters that reach the same $\dw$,
the subspace they occupy is immaterial to retention. This is the load-bearing
reconciliation of the paper: the axis worth controlling is a \emph{magnitude}, and
geometry is a \emph{fingerprint} of how a method lands on it---which the rest of this
section makes quantitative.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig2_fairness_residuals.png}
  \caption{\textbf{Geometry adds nothing (ANCOVA fairness test)---the test that
  separates this work from prior art.} Per-method retention residual from the
  pooled magnitude-law curve (the data itself is Figure~\ref{fig:hero}); the green
  band marks the ``indistinguishable from the law'' zone. Residual means: LoRA
  $+1.0$, LoRA-Null $+0.6$, LoRA{+}wd $+0.1$, MiLoRA $+1.0$, CLoRA $+0.1$, DoRA
  $+1.4$ (all n.s.); SC-LoRA $-4.15$\,pp ($p\!=\!0.006$, the only significant
  deviator on this seed---seeds 43/44 and an eval-matched calibration control
  both return it to the curve; see Limitations). Six free
  per-method intercepts buy $\Delta R^2\!=\!0.13$ ($F(6,41)\!=\!7.1$, seed-42
  statistic).}
  \label{fig:fairness}
\end{figure*}

\subsection{Geometry as a fingerprint}\label{sec:fingerprint}
The same SVD-alignment metrics that fail as a \emph{predictor} succeed as a
\emph{diagnostic}. Reconstructing each trained update $\Delta W=(\alpha/r)BA$ from the
320 saved adapters (no retraining) and projecting it onto the base weight's own
singular subspaces recovers, layer by layer, what each method was designed to
do---and the signature persists through three epochs of training
(Figure~\ref{fig:geometry}). MiLoRA is the only arm whose update energy leans toward
the base weight's \emph{minor} singular directions ($e_{\rm bot}\!=\!0.115$ vs.\
$e_{\rm top}\!=\!0.067$), its stated minor-init design; LoRA-Null concentrates on the
output-side principal directions ($e_{\rm top}\!=\!0.126$); SC-LoRA spikes on the
\emph{input-side} principal directions ($e^{\rm in}_{\rm top}\!=\!0.41$ on average,
exceeding $0.9$ in the early $q/k$ projections at low learning rate); while plain LoRA,
LoRA{+}wd, CLoRA, and DoRA sit near the $\approx\!0.06$ random-alignment baseline. The
fingerprint pays off diagnostically rather than by positing a geometric law:
PiSSA's principal-space init starts the adapter where the loss gradient is
largest, so it accumulates the biggest update in the study and is its worst
forgetter---a magnitude effect the fingerprint makes legible. One further
fingerprint texture is worth recording as an aside: SC-LoRA's input-principal
concentration \emph{decays} as the
learning rate rises ($e^{\rm in}_{\rm top}:\,0.70\!\to\!0.21$, $r\!=\!-0.96$;
Figure~\ref{fig:geometry}D)---exactly the constraint erosion its own paper warns
of. We record the erosion as a fingerprint observation only: the below-curve
retention it was originally invoked to explain proved to be a seed-and-calibration
artifact (seeds 43/44 and the eval-matched calibration both return SC-LoRA to the
curve; see Limitations), so we do not claim it as a forgetting mechanism.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig_geometry_4panel}
  \caption{\textbf{Geometry: a second-order fingerprint, not a separate lever}
  (Llama-2, from the reconstructed update $\Delta W=(\alpha/r)BA$ of the 320 saved
  adapters). \textbf{(A)} Retention vs.\ $\dw$ across methods---magnitude is the
  first-order lever (CorDA excluded from the fit). \textbf{(B)} Stress test: partial
  correlation of retention with each candidate geometric metric, controlling for
  $\log\dw$, computed over [all adapters] / [drop PiSSA{+}SC-LoRA] / [on-curve six].
  Two of the three principal-alignment terms (amp$_{\rm top}$,
  $e^{\rm in}_{\rm top}$) \emph{flip sign} ($+0.31$/$+0.41$) and the third
  ($e_{\rm top}$) collapses to $-0.17$ once the two principal-init
  outliers are removed; only stable rank (the modest rank lever) survives.
  \textbf{(C)} Per-method fingerprint (z-scored alignment metrics): each method's
  advertised design is recovered from its trained weights---MiLoRA minor
  ($e_{\rm bot}$), SC-LoRA input-principal ($e^{\rm in}_{\rm top}$), CorDA input-minor
  ($e^{\rm in}_{\rm bot}$), LoRA-Null output-principal. \textbf{(D)} SC-LoRA's
  input-principal concentration is an early-layer $q/k$ spike (reaching $\sim\!0.9$
  at low learning rate) whose overall level \emph{erodes} as the learning rate rises
  (inset, $e^{\rm in}_{\rm top}\!:0.70\!\to\!0.21$, $r\!=\!-0.96$)---a fingerprint
  texture; the seed-42 below-curve retention it was once invoked to explain
  resolves as a seed/calibration artifact (see Limitations). CorDA is
  shown as a fingerprint only and is
  excluded from every quantitative claim (see Limitations).}
  \label{fig:geometry}
\end{figure}

\paragraph{Rank is a modest second lever; static-SVD direction is not.} Two further
observations locate what does and does not matter beyond $\dw$. \emph{(i)} Rank acts
largely \emph{through} $\dw$---raising the LoRA rank raises $\dw$ (with $\sigma_1$
growing) and drives retention down---but a residual effect survives at matched
magnitude: over the registry's rank-sweep cells ($r\,4$--$256$ single-seed
plus $r\,8$--$128$ three-seed), the partial correlation of retention with rank
given $\log\dw$ is $-0.69$ to $-0.74$ in raw rank, and $-0.41$ once method
identity is also controlled. We flag this as spec-sensitive rather than headline
it: the partial is largely collinear with the update's stable rank, and it
\emph{reverses sign} (to $+0.7$--$+0.99$) if rank enters in log units---so the
honest statement is a residual negative rank effect (partial
$r\!\approx\!-0.4$ to $-0.7$ depending on specification, sign-unstable under
log-rank), second to magnitude.
The folklore that low rank alone mitigates forgetting
largely folds into the magnitude law. \emph{(ii)} The direction of the update in the
static weight-SVD basis---a leakage thermometer $\mu_E$ (the output leak into the
preserved subspace), exactly the quantity the static-SVD adapters (PiSSA, MiLoRA,
OPLoRA) reason about---predicts retention at $r\!\approx\!-0.09$, i.e.\ not at all.
The lever that family optimizes is uninformative for retention once magnitude is
fixed.\footnote{The rank-sweep cells (\texttt{lora\_r4}--\texttt{r256},
\texttt{mtx\_lora\_r8}--\texttt{r128}) carry \texttt{retention\_mean} and
\texttt{fdelta} in the released registry, so the rank partial is recomputable
from it; the $\mu_E$ leakage measurement is a companion experiment that predates
the registry, and the 320-adapter drift battery is
computed from the saved adapter weights (Appendix~\ref{app:repro}).}

\paragraph{CorDA appears here only as a fingerprint.} CorDA's own signature---an
input-side \emph{minor}-direction spike ($e^{\rm in}_{\rm bot}\!=\!0.49$, in the MLP
down-projections) together with a large-magnitude blow-up---is shown in
Figure~\ref{fig:geometry}C for completeness, but CorDA is \emph{excluded from every
quantitative claim in this paper} (law fit, Pareto table, ANCOVA residual): it is
mis-calibrated under the current setup and its $\dw$ explodes, so its position
carries no on/off-law meaning (see Limitations). It is a fingerprint, not an
assessed data point.

Six of the seven assessed adapters are on the law, and the seventh joins them
once its confounds are removed: SC-LoRA's seed-42 deviation is resolved by two
independent lines of evidence---seeds 43/44 place its operating-point retention
back on the curve ($25.6$/$25.8$ vs $22.5$), and an eval-matched calibration
control lifts it to $26.5$--$27.0$, at or above the base
ceiling (see Limitations)---so what looked like the study's one
geometric penalty is a seed-and-calibration artifact, which \emph{strengthens}
the geometry-null result. Math corroborates the resolution from a second
domain: SC-LoRA's three-seed math cell (GSM8K $60.9\!\pm\!0.4$, retention
$18.1\!\pm\!0.2$ at $\dw\!\approx\!0.86$) sits on the faithful-recipe math law
($n\!=\!48$, $r\!=\!-0.90$) with a residual of only $-1.8$\,pp---on math, too,
SC-LoRA's forgetting is its magnitude, not its subspace. The summary
measurement stands: an in-sample $R^2$ gain of only $0.13$ from six free
per-method intercepts (itself SC-LoRA-driven and seed-42-specific)---with
intercepts, slopes, and out-of-sample prediction all
indistinguishable across the on-curve adapters---plus a subspace-alignment
increment never exceeding $\Delta R^2\!\approx\!0.02$ on any subset (and
$0.0002$ for the strongest single candidate axis), is the direct measurement of
how little geometry buys once
magnitude is held fixed.

% ===========================================================================
\section{Learning Rate Is Only a Proxy}\label{sec:lr}
% ===========================================================================
\emph{Geometry does not change the shape of the retention curve; it changes only
where on the curve a given learning rate lands you.} That sentence is the
diagnosis (Claim~2), and this section establishes it. If forgetting is governed by
$\dw$, then learning rate should matter only insofar as it sets $\dw$---and,
critically, different adapters should transmit the \emph{same} learning rate into
\emph{different} $\dw$. Both hold, and together they are the mechanism of the
reported ``wins.''

\paragraph{$\dw$ beats learning rate as a predictor.} Regressing retention on
$\log(\text{learning rate})$ gives $R^2\!=\!0.32$ ($r\!=\!-0.57$); regressing it on
$\log(\dw)$ gives $R^2\!=\!0.74$ ($r\!=\!-0.86$). The variance explained more than
doubles when the $x$-axis switches from the learning rate to the update magnitude it
produces (Figure~\ref{fig:proxy}). Because a data-aware init starts the adapter
where the loss gradient is large, at a fixed learning rate it accumulates a
\emph{larger} $\dw$ than a vanilla init (Figure~\ref{fig:proxy}, panel A); geometry
does not change the \emph{shape} of the retention curve, it changes \emph{where on
the curve} a given learning rate lands you.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig7_lr_is_the_proxy.png}
  \caption{\textbf{Learning rate is only a proxy; $\dw$ is what governs retention.} Panel A: the
  same learning rate maps to different $\dw$ depending on the init (data-aware inits
  turn a fixed LR into a larger update). Panels B and C share a $y$-axis: retention
  versus learning rate is loose ($R^2\!=\!0.32$), retention versus $\dw$ is tight
  ($R^2\!=\!0.74$).}
  \label{fig:proxy}
\end{figure*}

\paragraph{The best learning rate is not shared.} Each method peaks at a different
learning rate: SC-LoRA at 5e-5, DoRA at 2e-4, LoRA and MiLoRA at 3e-4, LoRA{+}wd,
LoRA-Null, and CLoRA at 5e-4 (Figure~\ref{fig:lrsens}, Appendix~\ref{app:figs}).\footnote{These
best-adapt rates use the unconstrained ``max adaptation'' rule of Table~\ref{tab:cs};
the LR-artifact comparison in Table~\ref{tab:lr_artifact} uses a
retention-constrained rule (max adaptation subject to core retention $\ge24$), which
is why SC-LoRA's best-looking rate appears there as 2e-5 (its 5e-5 peak sits below
the retention floor) rather than 5e-5.} A single fixed learning rate therefore
flatters whichever method happens to be well tuned at that rate---precisely the
mechanism by which single-LR comparisons produce ``wins,'' and what our per-method
sweep is built to neutralize.

\paragraph{The competitor comparisons fix one rate.} This is not a hypothetical
failure mode. Most of the adapter comparisons we reproduce hold the learning rate
fixed and untuned across all methods, and state so: MiLoRA~\citep{wang2024milora},
CLoRA~\citep{clora}, LoRA-Null~\citep{tang2025loranull}, and
CorDA++~\citep{yang2025cordapp} each use a single shared learning rate for every arm
(MiLoRA and CLoRA inherit the LLM-Adapters commonsense rate; LoRA-Null and CorDA++
fix $2\text{e-}5$ for all methods), and none reports the $\dw$ that rate produces.
SC-LoRA~\citep{luo2025sclora} is the partial exception---it tuned its baselines'
learning rate in two of its three experiments---but even there its structured arm is
held fixed. A per-method sweep with $\dw$ logged is thus exactly the control this
literature omits.

\paragraph{The single-LR illusion, made explicit.} We can now show the illusion
directly rather than assert it (Figure~\ref{fig:lrartifact},
Table~\ref{tab:lr_artifact}). For each structured adapter we take its
\emph{best-looking single learning rate}---the rate that maximizes commonsense
adaptation while keeping core retention $\geq\!24$---and compare it against plain
LoRA \emph{run at that same rate}. At that fixed rate several methods do look better
than LoRA: SC-LoRA by $+26.0$\,pp of adaptation, LoRA-Null by $+19.5$, DoRA by
$+5.9$, MiLoRA by $+0.8$ (the illusion is strong for the low-LR-optimal data-aware
inits, weak for MiLoRA, and \emph{absent} for CLoRA, which is $-14.2$ even at its own
best rate). But once the learning rate is swept, the LoRA{+}wd frontier sits on or
above every one of these best single-LR points on \emph{both} axes in this
single-seed sweep (Table~\ref{tab:lr_artifact}). The retention margins over these
best single-LR points are sub-1\,pp, and because the structured adapters'
operating points are themselves seed-fragile we leave them at ``matches/edges''
(see Limitations)---though against plain LoRA, where three seeds exist on both
sides, the swept LoRA{+}wd edge is outside seed noise on both axes
(Section~\ref{sec:pareto}); the
load-bearing point is not the size of the gap but that \emph{no fixed-LR advantage
survives a symmetric sweep}. The apparent per-method win thus decomposes into a
learning-rate-tuning effect that vanishes under the sweep, plus a residual that
lands back on the shared magnitude curve. This is the concrete, per-method form of
Claim~2.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig9_lr_artifact.png}
  \caption{\textbf{The ``win'' of every structured adapter carries the ingredients
  of a learning-rate artifact.} Full 7-LR trajectory per method in the
  adaptation--retention plane (Llama-2-7B, commonsense, seed 42). Rings mark each
  method's best-looking single-LR point; the shaded region is on or below the
  LoRA{+}wd \emph{swept} frontier (thick blue). At high learning rate the data-aware
  inits collapse (bottom cluster: magnitude blow-up). CorDA is withheld from this
  exhibit (see Limitations).}
  \label{fig:lrartifact}
\end{figure}

\input{tables/table_lr_artifact_paper.tex}

\paragraph{The sweep is symmetric.} An obvious referee question is whether we tuned
LoRA{+}wd as hard as the methods it beats. We did: every method, including
LoRA{+}wd, receives the same seven-rate sweep, and each is reported at its own
best-adapt learning rate. The instrument is symmetric by construction. What this
exhibit reproduces is the single-LR \emph{illusion from our own sweep}; the fully
airtight form---taking a fancy adapter's \emph{paper-reported} published-LR win over
LoRA and dissolving it under the sweep---is deferred
(see Limitations), and we do not claim it here; the seed-averaging half
of that program has since landed (three-seed triples on the headline operating
points; see Limitations), and it moves no conclusion.
%TODO(experiment): reproduce-a-published-win-then-dissolve-it still pending
%(seed-averaging done: lrsw_*_s43/s44 landed on all 7 headline cells, DoRA s44
%included as of 2026-07-14).

% ===========================================================================
\section{The Practical Corollary: Weight Decay Bounds $\dw$ for Free}\label{sec:pareto}
% ===========================================================================
The magnitude law has a deflationary practical corollary. If $\dw$ is the lever,
then the simplest possible control of it---a weight-decay term on the adapter
matrices, with no geometric machinery at all---should land on the frontier of the
elaborate adapters. It does, and the way it does so is the point:
\textbf{LoRA{+}wd wins by moving \emph{along} the shared curve to a smaller
magnitude, not by beating the curve.} Its ANCOVA residual is $+0.06$\,pp---dead on
the law (Section~\ref{sec:ancova}).

\paragraph{The mechanism: a two-edged budget.} $\dw$ is a budget with two edges.
Adaptation \emph{rises} with $\dw$ (at $+20.3$\,pp/decade) while retention
\emph{falls} (at $-14.8$\,pp/decade), so there is a band of $\dw$ in which
adaptation is near its maximum while retention stays near the base ceiling
(Figure~\ref{fig:budget}). Adapters fall off the frontier when their
(init~$\times$~learning-rate) combination pushes $\dw$ past the right edge of that
band. Weight decay penalizes $\lVert A\rVert^2 + \lVert B\rVert^2$ regardless of
subspace, so it \emph{bounds} $\dw$ and lands the model inside the band ``for
free'': LoRA{+}wd(0.3) carries the smallest update in the comparison
($\dw\!=\!0.394$), while the un-regularized high-adapters sit at larger magnitudes.
The ``for free'' is literal on compute as well as engineering---weight decay is a
single optimizer flag that adds no initialization pass and leaves training wall-clock
indistinguishable from plain LoRA, whereas the geometric adapters pay a
data-aware-initialization or per-step regularizer tax for the same trainable budget
(Appendix~\ref{app:repro}).
That is the whole mechanism of Claim~3---the geometric adapters are elaborate ways
to choose \emph{where} the update goes; weight decay is the trivial way to choose
\emph{how big} it is, and how big is what matters.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig8_magnitude_budget.png}
  \caption{\textbf{One axis sets both adaptation and forgetting.}
  Adaptation (top, rising, $+20.3$\,pp/decade) and retention (bottom, falling,
  $-14.8$\,pp/decade) versus $\dw$ (shared log $x$-axis). The green band annotates
  the descriptive sweet spot $\dw\!\in\![0.31,0.62]$ (read off these points, not
  fitted); LoRA{+}wd (0.394) sits inside it.}
  \label{fig:budget}
\end{figure}

\paragraph{On the frontier, both domains.} Table~\ref{tab:cs} reports each method at
its own adaptation-optimal learning rate on commonsense (the fair, per-method-tuned
comparison), and Figure~\ref{fig:pareto} plots the plane. LoRA{+}wd(0.3) at lr5e-4
reaches CS-8 $=\!81.6$ and core retention $=\!25.6$ (within $0.4$\,pp of the base
ceiling) at the lowest magnitude in the table---simultaneously the strongest adapter
and the best retainer among the strong adapters. Seeds 43/44 tighten rather than
erode this: the three-seed operating point is CS-8 $81.80\!\pm\!0.16$ / retention
$25.93\!\pm\!0.42$ at $\dw\!=\!0.38$--$0.41$, versus plain LoRA's
$79.08\!\pm\!0.11$ / $23.81\!\pm\!0.58$---an edge on \emph{both} axes outside
three-seed noise against plain LoRA (against the elaborate adapters the verb
remains ``matches or edges,'' below). A param-matched control closes the
capacity reading of this result: LoRA{+}wd at rank~16 ($28.0$M trainable
parameters, matched to plain LoRA) reproduces the operating point at half the
parameters (CS-8 $81.04$ / retention $26.27$ at $\dw\!=\!0.334$), while a
plain-LoRA rank ladder (r8${\to}$r16${\to}$r32 at fixed lr\,3e-4) slides
\emph{down} the shared curve ($\dw\ 0.518\!\to\!0.603\!\to\!0.739$, retention
$24.0\!\to\!23.0\!\to\!22.2$)---the operating point is bought by the wd knob,
not the extra rank, and the rank effect vanishes once $\dw$ is controlled
(see Limitations for a disclosed adaptation-collapse sibling cell). Math
(Table~\ref{tab:math})
confirms it under each method's own faithful published recipe (MetaMathQA,
cutoff 256): LoRA{+}wd(0.3) at lr2e-4 reaches GSM8K $66.8\!\pm\!0.8$ with BBH
$33.6\!\pm\!1.0$ (three seeds, retention at or above the $33.1$ no-fine-tuning
base in each)---every seed above the best published GSM8K among the competitors'
Llama-2-7B tables ($64.6$, CLoRA-k128, their own harness)---and, \emph{within our
harness} (seed-42 cells on
both sides), $+6.5$\,pp GSM8K
over our own CLoRA-k256 reproduction ($60.8$) with $+4.5$\,pp BBH, at a quarter
of its update magnitude ($\dw\ 0.28$ vs $1.02$). We compare only within-harness;
cross-harness GSM8K deltas (each paper's own evaluation code and recipe) are not
meaningful beyond ``matches/edges.''

\paragraph{Robustness, not just the peak.} The single-seed peak comparison is the
noisiest part of the evidence; the robustness column of Table~\ref{tab:cs} is the
sturdiest, because it counts over all seven independently trained runs of a method
rather than resting on any single cell. Bounding $\dw$ does not just move the best
point---it widens the band of learning rates that are safe at all: LoRA{+}wd keeps
core retention $\geq\!24$ at \textbf{6 of 7} learning rates (plain LoRA, LoRA-Null,
MiLoRA, and CLoRA 5 of 7; DoRA 4 of 7), whereas SC-LoRA does so at \textbf{1 of 7}.
A practitioner picking a rate blind is safe almost anywhere on the LoRA{+}wd
sweep and almost nowhere on SC-LoRA's (full operating points:
Table~\ref{tab:oppoints}, Appendix~\ref{app:figs}).

\begin{table*}[t]
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \input{tables/table_main_cs_paper.tex}
  \caption{\textbf{Main commonsense results} (Llama-2-7B, seed 42), each method at
  its best-adapt learning rate. LoRA{+}wd(0.3) is simultaneously highest-adaptation
  (81.6), highest core-retention among the strong adapters (25.6, essentially the
  base ceiling 26.0), lowest-magnitude ($\dw\!=\!0.394$), and widest-band (6 of 7
  learning rates keep core retention $\geq\!24$). Seeds 43/44 on these operating
  points confirm the ranking where it matters: LoRA{+}wd $81.80\!\pm\!0.16$ /
  $25.93\!\pm\!0.42$; plain LoRA $79.08\!\pm\!0.11$ / $23.81\!\pm\!0.58$; CLoRA
  $78.41\!\pm\!0.11$ / $21.58\!\pm\!0.48$; LoRA-Null $78.93\!\pm\!0.12$ /
  $22.14\!\pm\!1.32$. \emph{Caveat (seed-fragile cells):} MiLoRA's CS-8 79.9
  (marked $^{\dagger}$ in the table) is a lucky seed---its three-seed adaptation
  is $57.7\!\pm\!22.7$
  ($79.9/58.7/34.5$, a collapse basin) while its retention is stable
  ($24.2\!\pm\!0.5$); SC-LoRA's row is also seed-sensitive via $\dw$
  ($0.56$ vs $0.29/0.28$), with retention tracking the law in each case
  (see Limitations). Ret-broad is calibrated against its $35.26$
  no-fine-tuning ceiling. CorDA excluded pending re-validation
  (see Limitations).}
  \label{tab:cs}
\end{table*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig3_pareto.png}
  \caption{\textbf{Best operating point per adapter}, commonsense (left) and math
  (right); each method at its own best-adapt learning rate, with the base ceiling
  in green and the LoRA{+}wd point starred. On commonsense, LoRA{+}wd's best-adapt
  point is highest on both axes among the high-adaptation adapters (its edge over
  plain LoRA is outside three-seed noise; margins over the elaborate adapters
  remain ``matches or edges,'' the Limitations section). The math panel plots
  the \emph{sweep-recipe} arm, now the full five-adapter LR sweep (LoRA,
  LoRA{+}wd, DoRA, CLoRA, MiLoRA; 7 LRs each, seed 42, $n{=}35$); LoRA{+}wd
  again leads both axes (GSM8K $50.6$, retention $24.6$ at lr5e-4). The
  headline math comparison is the
  faithful-recipe grid of Table~\ref{tab:math}, where LoRA{+}wd edges every
  method on both axes. Full learning-rate trajectories:
  Figure~\ref{fig:lrartifact}.}
  \label{fig:pareto}
\end{figure*}
% All figures regenerated 2026-07-02 (second pass) from the clean 320-row registry
% (campaign_summary_clean.jsonl) under the SPLIT LoRA-Null convention
% (writing/make_figs_split_lora_null.py); stats verified against key_numbers.md
% sections 14-15.

\begin{table*}[t]
  \centering
  % Inline table (supersedes tables/table_main_math.tex, which carried the older
  % cutoff-limited lrswm_ sweep numbers, GSM8K 50.6 etc.). Source rows:
  % results/frm_*_c256_s42/summary.json (+ s43/s44 for the LoRA+wd headline);
  % verified 2026-07-11 (author_fix_log_sat.md).
  \begin{tabular}{l l c c c}
  \toprule
  Method & Config & GSM8K $\uparrow$ & BBH (retention) $\uparrow$ & $\dw$ $\downarrow$ \\
  \midrule
  \textit{Base (no-FT)} & -- & -- & \textit{33.1} & \textit{0} \\
  \midrule
  LoRA+wd(0.3) & r64, lr2e-4 & \textbf{67.3} {\footnotesize(3-seed $66.8\!\pm\!0.8$)} & \textbf{33.1} {\footnotesize(3-seed $33.6\!\pm\!1.0$)} & \textbf{0.28} \\
  LoRA (wd 0)  & r64, lr1e-4 & 65.0 & 31.0 & 0.43 \\
  MiLoRA       & r64, lr1e-4 & 62.9 & 32.4 & 0.45 \\
  CLoRA-$k$256 & r64, lr3e-4 & 60.8 & 28.6 & 1.02 \\
  LoRA (recipe LR) & r64, lr3e-4 & 60.2 & 29.1 & 1.28 \\
  DoRA         & r64, lr3e-4 & 58.8 & 29.3 & 2.84 \\
  PiSSA        & r64, lr3e-4 & 49.7 & 7.2  & 2.21 \\
  \bottomrule
  \end{tabular}
  \caption{\textbf{Main math (GSM8K) results under the faithful published recipe}
  (Llama-2-7B, MetaMathQA, cutoff 256, seed 42 unless noted; retention reported
  as BBH against the $33.1$ no-fine-tuning base). LoRA{+}wd(0.3) and MiLoRA are
  shown at their best-adapt learning rate over the swept rates; CLoRA, DoRA, and
  PiSSA at the recipe's published rate (lr3e-4). The LoRA{+}wd headline cell is
  three-seed ($66.8\!\pm\!0.8$ / $33.6\!\pm\!1.0$); all comparisons are
  within-harness. The full-grid c256 maximum is actually $67.4$ (wd0.2, lr1e-4,
  s42), but that cell is seed-fragile ($67.4/64.5/64.3$), so the headline uses
  the seed-stable wd0.3 cell. LoRA{+}wd edges every arm on both axes at the
  smallest update in the table.}
  \label{tab:math}
\end{table*}

\paragraph{The honest verb, and why the margin is not the point.} Against the
elaborate geometric adapters we claim that
LoRA{+}wd \emph{matches or edges} the frontier at far lower engineering cost---not
that it \emph{dominates}. An earlier full-scale read that looked like domination
(``LoRA{+}wd dominates CLoRA'') deflated to a tie once a checkpoint-collision data
bug was fixed, and several of those methods' operating points are themselves
seed-fragile (MiLoRA's adaptation basin, SC-LoRA's bimodal $\dw$;
Table~\ref{tab:cs}), so per-method deltas against them are not stable objects.
Against \emph{plain LoRA}, where three seeds exist on both sides, the verb can be
stronger: LoRA{+}wd edges it on both axes outside three-seed noise
($81.80\!\pm\!0.16$ vs $79.08\!\pm\!0.11$; $25.93\!\pm\!0.42$ vs
$23.81\!\pm\!0.58$). But under the corollary framing the margin is not the
claim: the result is not that LoRA{+}wd \emph{ranks first}, it is that a
subspace-free magnitude knob \emph{reaches the frontier at all}, which is exactly
what the magnitude law predicts and what the geometric machinery was supposed to be
necessary for.

% ===========================================================================
\section{Discussion and Conclusion}\label{sec:conclusion}
% ===========================================================================
The field is optimizing a second-order variable. Catastrophic forgetting under PEFT
is governed to first order by a single scalar, the magnitude of the weight update
$\dw$; geometry---the choice of \emph{where} the update goes---acts largely
\emph{through} that magnitude and, once it is controlled, adds essentially nothing
measurable beyond it (no subspace-alignment metric buys more than
$\Delta R^2\!\approx\!0.02$, and the strongest single candidate axis buys
$0.0002$, n.s.; the one apparent exception, SC-LoRA's seed-42 point, dissolved
under seed replication and an eval-matched calibration control;
Section~\ref{sec:geometry}). The diagnosis exhibits the
\emph{ingredients} of a learning-rate artifact behind the reported wins: each
adapter peaks at a different rate, and the $\dw$ it produces predicts retention
far better than its learning rate does, so a per-method sweep collapses the
difference back onto the shared curve. The practical consequence is deflationary:
plain LoRA with a weight-decay term matches or edges the adaptation--retention
frontier of every elaborate geometric adapter we tested (and, against plain LoRA,
edges it on both axes outside three-seed noise)---and holds the widest
safe learning-rate band while doing so---not by ranking first but by reaching a
frontier the geometric machinery was supposed to be necessary for; it simply moves
along the shared curve to a smaller magnitude.

Each claim is stated at the strength its evidence supports. The law (Claim~1) is
complete and robust on Llama-2. The diagnosis (Claim~2) is supported at the
honest strength of ``best-LR-is-not-shared.'' Three of the controls originally
deferred for the consequence (Claim~3) have since landed and strengthened it:
three-seed error bars on the headline cells put LoRA{+}wd's edge over plain LoRA
outside seed noise on both axes, the eval-matched calibration resolved
SC-LoRA's deviation, and the param-matched rank-16 control reproduced the
LoRA{+}wd operating point at half the parameters, closing the capacity
confound (Section~\ref{sec:pareto}). The Qwen math grid has landed as well and
replicates the law's direction (Section~\ref{sec:law}). Against the elaborate
adapters the verb remains
``matches/edges'' until the still-outstanding controls land: CorDA re-instated
(or openly declared off-curve), Qwen commonsense seeds, and the Qwen base
ceilings. Should those controls change the
rankings, the paper still stands as a \emph{measurement} contribution: the
learning-rate-sweep-per-method decomposes any adapter's reported win into its true
magnitude effect plus a learning-rate-tuning artifact, and the
effective-update-magnitude axis $\dw$ is the fair way to compare adapters.

\paragraph{Future work.} Beyond the controls in the Limitations section, three
directions follow naturally. \emph{(i) CorDA++}~\citep{yang2025cordapp} (dynamic
covariance selection and dynamic rank allocation under a fixed parameter budget)
is the strongest 2025-era geometric method and the right target to land the
negative result against, at a realized-parameter-count matched budget.
\emph{(ii) Eval-matched calibration arms} would settle whether the data-aware
inits' residual penalty is geometry or a calibration artifact---if they return to
the curve, the cleaner result is ``the law is method-free once calibration is
fair.'' \emph{(iii) A gated-magnitude adapter}---an input-conditional gate scaling
only the adapter delta, trained against a preservation corpus disjoint from the
evaluation set---is the constructive follow-up, now with the higher bar of having
to beat LoRA{+}wd rather than the elaborate adapters.

The message for a field that ships a new adapter every week is a wake-up call:
\textbf{control the size of the update first---geometry sets where on the magnitude
curve a method starts, not a separate axis of forgetting.}

% ===========================================================================
\section*{Limitations}
% ===========================================================================
Table~\ref{tab:claims} maps each claim to the strength of its current evidence and
to the named experiment that closes its gap; the items below give the detail.

\input{tables/table_claims_evidence.tex}

\begin{enumerate}
  \item \textbf{Seeds: three-seed on the headline cells; single-seed elsewhere.}
        Seeds 43/44 have landed on the headline operating points of all seven
        assessed adapters (DoRA's seed-44 sibling landed last, so all seven
        commonsense operating points are now three-seed), and they sharpen
        rather than soften the picture: LoRA{+}wd's edge
        over plain LoRA is outside three-seed noise on both axes
        ($81.8\!\pm\!0.2$ vs $79.1\!\pm\!0.1$; $25.9\!\pm\!0.4$ vs
        $23.8\!\pm\!0.6$), SC-LoRA's below-curve point regresses to the curve
        ($22.5\!\to\!25.6/25.8$; three-seed $24.6\!\pm\!1.8$), and the
        ``collapse basins'' a prior three-seed matrix warned about are real
        (MiLoRA's CS-8 is $57.7\!\pm\!22.7$; Table~\ref{tab:cs}). Deltas against
        the \emph{elaborate} adapters therefore stay at ``matches/edges''---their
        operating points are themselves seed-fragile---and everything beyond the
        headline cells remains seed-42 only: the full 49-cell grid, the
        ANCOVA/geometry battery (the near-circularity item below), and the Qwen
        arm.
  \item \textbf{Rank and knob asymmetry: the capacity control has landed.}
        Ranks are not matched
        (LoRA/DoRA/CorDA/LoRA-Null r16; MiLoRA/SC-LoRA r32; CLoRA $k{=}1024$), and
        \emph{only} LoRA received the weight-decay knob, so a referee might say
        LoRA{+}wd wins because it has more capacity and an extra regularizer no
        other method got. The param-matched control (B5a) now closes the
        capacity half of that objection: LoRA{+}wd at rank~16 ($28.0$M trainable
        parameters, matched to plain LoRA) reproduces the rank-32 operating
        point at half the parameters---CS-8 $81.04$ / core retention $26.27$ at
        $\dw\!=\!0.334$, against the r32 point's $81.6$/$25.6$ at $0.394$---and
        a plain-LoRA rank ladder at fixed lr\,3e-4 (r8${\to}$r16${\to}$r32)
        moves \emph{along} the curve rather than off it
        ($\dw\ 0.518\!\to\!0.603\!\to\!0.739$ with retention
        $24.0\!\to\!23.0\!\to\!22.2$): the rank effect vanishes once $\dw$ is
        controlled---rank buys magnitude, not retention. One disclosed anomaly:
        the rank-16 control's lr\,3e-4 sibling lands in a deterministic
        answer-format collapse basin---CS-8 $13.53$, below chance per-dataset,
        with retention fully \emph{intact} ($26.84$) and healthy training, and
        a dedicated re-evaluation reproduces it; it is not a magnitude effect
        ($\dw\!=\!0.328$), and its seed replicate is queued. The control is
        single-seed, and the weight-decay knob has still been given only to
        LoRA---so the remaining strong form of Claim~3 is the wd knob extended
        to every frontier arm.
  \item \textbf{Calibration-versus-evaluation mismatch (now tested directly for
        SC-LoRA).} CorDA, SC-LoRA, and LoRA-Null calibrate on nq\_open (factoid
        QA), but retention is measured on academic/reasoning benchmarks. A
        data-aware init only protects the directions its calibration covariance
        exercises, so nq\_open may protect the wrong subspace for our
        evaluation---meaning ``data-aware inits forget more than their budget''
        could be a calibration artifact rather than a real geometric penalty. For
        SC-LoRA this is no longer hypothetical: an eval-matched calibration
        control (four cells) lifts its retention at the same operating point from
        $22.5$ to $26.5$--$27.0$, at or above the $26.0$ base ceiling, converging
        with the seed evidence (the seeds item above) on the same verdict---the
        seed-42 deviation was a seed-and-calibration artifact, not a geometric
        penalty. The control has not yet been extended to CorDA or LoRA-Null
        (that expansion arm is queued), so calibration sensitivity for those two
        remains open.
  \item \textbf{CorDA is withheld from every quantitative claim.} CorDA was first
        mis-calibrated on wikitext-2 and is being re-run on nq\_open; it is excluded
        from every law figure, Pareto table, ANCOVA residual, and the
        learning-rate-artifact diagnostic (Figure~\ref{fig:lrartifact}), and its 7
        cells are not among the 49 clean cells. It appears in only one exhibit, the
        geometry fingerprint (Figure~\ref{fig:geometry}, Section~\ref{sec:geometry}),
        and there \emph{only} as a qualitative fingerprint---explicitly labelled as
        excluded from every quantitative law/table claim---never as an assessed
        on/off-curve result. Because CorDA is arguably the most important adapter for
        the ``geometry adds nothing beyond magnitude'' claim, its absence from the
        quantitative set is a real gap: the study assesses seven of its eight
        adapters, six of them on-curve, not all eight.
        %TODO(experiment): CorDA nq_open re-run + passing 0-step self-check + fair
        %(B4) calibration required before CorDA can re-enter the law.
  \item \textbf{Math and Qwen law fits are single-seed; Qwen math is
        qualitative, not quantitative} (Section~\ref{sec:law}). Math is a
        genuine second domain---five adapters
        at seven rates on the sweep ($n\!=\!35$, $r\!=\!-0.95$) plus the
        faithful-recipe block---but its law fits are seed-42 only (six
        faithful-recipe math configurations, including the LoRA{+}wd headline
        cell, are now three-seed, with retention SDs $\le\!0.53$ across all
        six). Qwen commonsense is a complete
        single-seed second-architecture replication ($n\!=\!49$, pooled
        $r\!=\!-0.86$); the Qwen math grid now replicates the law's direction
        ($n\!=\!56$ assessed s42 cells on BBH retention, $r\!=\!-0.70$, slope
        $-14.4$\,pp/decade over $\sim$1.5 decades, two diverged cells excluded
        with disclosure), but we hold it at \emph{qualitative} strength: it is
        single-seed, BBH-only (MMLU-Pro is excluded by convention, having been
        unreliable on the early Qwen-math rows), and Qwen's no-fine-tuning
        ceilings are unmeasured, so neither the Qwen slope scale nor a
        quantitative law match can be asserted. Qwen commonsense seeds and the
        Qwen base ceilings remain the open items.
  \item \textbf{Base-ceiling calibration: landed for Llama-2; Qwen still open.}
        The no-fine-tuning evaluation (C5) has landed for Llama-2: MMLU
        $40.88$, ARC-Challenge $44.80$, TruthfulQA $38.85$, for a broad ceiling
        of $35.26$, with the same run reproducing the core ceiling at $25.89$
        against the canonical external $26.0$ (a confirming replicate). Broad
        retention and the per-benchmark slopes are therefore calibrated, and
        the TruthfulQA flatness is confirmed real rather than a floor artifact
        (fine-tuned values $31.4$--$39.5$ against base $38.85$: a constant
        $\approx\!-3$\,pp fine-tuning offset, magnitude-independent;
        Section~\ref{sec:law}). What remains uncalibrated is \emph{Qwen}: no
        Qwen2.5-7B no-fine-tuning ceiling has been measured, and the reading of
        Qwen's steeper slope ($-32$ vs $-14.8$\,pp/decade) as a scale
        difference leans on it.
  \item \textbf{Near-circularity of the raw correlation.} ``Bigger update forgets
        more'' is partly true by construction; we rebut by resting the claim on the
        residual/ANCOVA battery of Section~\ref{sec:ancova}---intercepts, slopes,
        and the leave-one-method-out predictive check---rather than on the raw
        correlation. Those tests remain single-seed with $n\!=\!7$ per method, and
        the overall significance of the full-pool tests ($F(6,41)\!=\!7.05$
        intercepts; $F(6,35)\!=\!9.32$ slopes) is entirely SC-LoRA-driven and
        specific to seed~42: at SC-LoRA's operating point seeds 43/44 give
        retention $25.6$/$25.8$ (three-seed $24.6\!\pm\!1.8$ against the $22.5$
        that drives the statistics), so a seed-averaged recompute would most
        likely render the full-pool tests non-significant---strengthening, not
        weakening, the geometry-inert conclusion. We nevertheless report the
        seed-42 battery as computed and rest the claim on the on-curve tests
        (jointly n.s.), which do not depend on SC-LoRA.
  \item \textbf{DoRA's magnitude coordinate is a lower bound.} We compute $\dw$
        from PEFT's \texttt{get\_delta\_weight}, which for DoRA returns only the
        low-rank product $B A \cdot\text{(scaling)}$ and omits DoRA's
        magnitude-vector rescaling, applied at forward time. DoRA's
        \emph{retention} is therefore measured correctly (evaluation runs the true
        forward pass), but its \emph{$\dw$ coordinate is under-measured}, so DoRA
        may sit slightly to the right of where we plot it. A corrected recompute
        requires re-running the seven DoRA cells (the checkpoints were not
        retained) and is deferred to camera-ready. The direction of the correction
        does not threaten the on-curve conclusion: DoRA currently sits on or above
        the curve (residual $+1.37$\,pp, Section~\ref{sec:ancova}), and moving it to
        a larger $\dw$---where the curve predicts \emph{lower} retention---can only
        increase its positive residual, not pull it below the law.
        %TODO(experiment): recompute DoRA fdelta with the magnitude-vector rescaling
        %(needs the 7 DoRA checkpoints + GPU); deferred to camera-ready.
\end{enumerate}

% ===========================================================================
% (bib style acl_natbib is set by acl.sty itself)
\bibliography{references}
% Fallback (kept commented): if references.bib is unavailable, uncomment below and
% comment out \bibliography{references}.
% \begin{thebibliography}{9}
% \bibitem[Hu et al.(2021)]{hu2021lora} E.~Hu et al. LoRA. arXiv:2106.09685, 2021.
% \bibitem[Liu et al.(2024)]{liu2024dora} S.~Liu et al. DoRA. arXiv:2402.09353, 2024.
% \bibitem[Yang et al.(2024)]{yang2024corda} Y.~Yang et al. CorDA. arXiv:2406.05223, 2024.
% \bibitem[Wang et al.(2024)]{wang2024milora} H.~Wang et al. MiLoRA. arXiv:2406.09044, 2024.
% \bibitem[Lu et al.(2024)]{clora} Y.~Lu et al. Controlled LoRA (CLoRA). arXiv:2410.16801, 2024.
% \end{thebibliography}

% ===========================================================================
\appendix
% ===========================================================================
\section{Supporting Figures}\label{app:figs}
% ===========================================================================
This appendix collects the supporting exhibits referenced from the main body: the
learning-rate-sensitivity curves (Figure~\ref{fig:lrsens}), the per-benchmark
degradation (Figure~\ref{fig:perbench}), the further supporting structure
(Figure~\ref{fig:support}), and the operating-point/robustness table
(Table~\ref{tab:oppoints}).

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig4_lr_sensitivity.png}
  \caption{\textbf{Learning-rate optima are not shared.} Adaptation (left) and
  retention (right) versus learning rate; rings mark each method's
  adaptation-optimal rate. The optimum ranges over
  $\{5\text{e-}5, 2\text{e-}4, 3\text{e-}4, 5\text{e-}4\}$ (Section~\ref{sec:lr}).}
  \label{fig:lrsens}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig5_per_benchmark.png}
  \caption{\textbf{Which knowledge dies first.} Each benchmark's accuracy versus
  $\dw$ (log), with the pooled slope; final panel is the slope bar chart. MMLU
  degrades fastest ($-23.4$\,pp/decade); TruthfulQA is flat ($-0.46$, n.s.\
  against its calibrated $38.85$ base ceiling---a constant $\approx\!-3$\,pp
  offset, not a floor artifact) (Section~\ref{sec:law}).}
  \label{fig:perbench}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig6_supporting_structure.png}
  \caption{\textbf{Supporting structure.} (a) Adaptation rises with $\dw$ (magnitude
  is required to adapt). (b) Spectral spikiness $\smax/\bar\sigma$ per method (why
  $\smax$ is an unfair axis). (c) Adaptation achievable at a fixed retention budget,
  per method (Section~\ref{sec:pareto}).}
  \label{fig:support}
\end{figure*}

\begin{table*}[t]
  \centering
  \input{tables/table_op_points.tex}
  \caption{\textbf{Operating points and robustness} (Llama-2-7B commonsense, seed
  42). Per method: the best-adapt learning rate (max adaptation, any retention),
  the safe learning rate (max adaptation with core retention $\geq\!24$, i.e.\
  base $-$ 2pp), and robustness (number of the 7 learning rates keeping core
  retention $\geq\!24$). LoRA{+}wd 6/7 (widest); plain LoRA, LoRA-Null, MiLoRA,
  and CLoRA 5/7; DoRA 4/7; SC-LoRA 1/7 (brittle). For CLoRA, LoRA-Null, and
  SC-LoRA the safe rate forces a large adaptation sacrifice.
  $^{\dagger}$MiLoRA's 79.9 adaptation cell is seed-fragile: three-seed
  $57.7\!\pm\!22.7$ ($79.9/58.7/34.5$), retention stable at $24.2\!\pm\!0.5$
  (Table~\ref{tab:cs}).}
  \label{tab:oppoints}
\end{table*}

\subsection{CLoRA published cross-check}\label{app:clora}
Table~\ref{tab:clora} collects the published Llama-2-7B numbers from
CLoRA~\citep{clora} that corroborate the magnitude law
(Section~\ref{sec:law}). CLoRA is the only competitor on our exact setup that also
reports forgetting on our exact axis (BBH and MMLU-Pro). We reproduce its numbers
here as \emph{triangulating} evidence, not as values to merge with ours: CLoRA's
base BBH ($34.91$) exceeds our answer-only base ($33.10$), a harness-configuration
gap, so the comparison is of relative degradation from each paper's own base, and
of the \emph{slope} of its $\mathbb{F}$ proxy---which is the same $F_\Delta$ metric
our $\dw$ implements (Section~\ref{sec:setup})---not of raw coordinates. On its own
scale, fitting all ten fine-tuned rows of Table~\ref{tab:clora}, retention (BBH)
falls with $\log_{10}\mathbb{F}$ at $r\!=\!-0.98$, pooled slope
$-14.65$\,pp/decade. The proximity to our $-14.8$\,pp/decade is band-level
agreement, not a point match (the pooled value blends two row families; see
below), and it holds even though CLoRA's absolute $\mathbb{F}$ levels run lower
than ours (a probe/harness offset). Two provenance
notes make the fit checkable. First, the no-fine-tuning \emph{Base} row
($\mathbb{F}\!=\!2.42$) is excluded by construction---it is a reference scale,
not a fine-tuned point, and the fit is sensitive to that rule (including it
collapses $r$ from $-0.98$ to $-0.59$), so we state it explicitly. Second, the pooled slope
decomposes into a $-12.7$\,pp/decade baseline family (rows 1--5) and a steeper
$-18.9$\,pp/decade $k$-series (rows 6--10), so the cross-paper claim is a
parallel-slope band ($-12.7$ to $-18.9$ bracketing our $-14.3$ to $-14.8$),
not a second-decimal coincidence. Reading down the
table, forgetting (BBH
falling below base) tracks the $\mathbb{F}$ proxy (larger $\mathbb{F}$,
more forgetting): the unregularized baselines have the largest $\mathbb{F}$ and
forget most, while
the $L^2$-regularized and CLoRA variants have smaller $\mathbb{F}$ and retain
more---the magnitude law, seen in a competitor's own data.

\begin{table*}[t]
  \centering
  \begin{tabular}{llcccc}
    \toprule
    \# & Method & BBH & $\mathbb{F}$ & $\log_{10}\mathbb{F}$ & In fit? \\
    \midrule
    -- & Base (LLaMA-2-7B) & 34.91 & 2.42 & $+0.384$ & no (reference row) \\
    \midrule
    1  & LoRA baseline     & 26.69 & 0.79 & $-0.102$ & yes \\
    2  & DoRA baseline     & 26.90 & 0.95 & $-0.022$ & yes \\
    3  & PiSSA baseline    & 26.73 & 1.03 & $+0.013$ & yes \\
    4  & LoRA-L2 ($L^2$ reg.) & 32.93 & 0.29 & $-0.538$ & yes \\
    5  & rsLoRA baseline   & 25.14 & 0.92 & $-0.036$ & yes \\
    6  & CLoRA-$k$128      & 30.82 & 0.36 & $-0.444$ & yes \\
    7  & CLoRA-$k$256      & 31.92 & 0.34 & $-0.469$ & yes \\
    8  & CLoRA-$k$512      & 34.32 & 0.27 & $-0.569$ & yes \\
    9  & CLoRA-$k$1024     & 36.49 & 0.21 & $-0.678$ & yes \\
    10 & CLoRA-$k$2048     & 38.67 & 0.14 & $-0.854$ & yes \\
    \bottomrule
  \end{tabular}
  \caption{\textbf{CLoRA's published Llama-2-7B Table~4, in full} (from
  \citealp{clora}, arXiv:2410.16801; extraction re-verified 2026-07-11). BBH
  measures forgetting on our exact axis;
  $\mathbb{F}=\lVert\Delta W x\rVert/\lVert x\rVert$
  is CLoRA's forgetting proxy---the same $F_\Delta$ metric our $\dw$ implements
  (Eq.~\ref{eq:fdelta}). The fit over the ten fine-tuned rows gives
  $r\!=\!-0.98$, pooled slope $-14.65$\,pp/decade (a blend: rows 1--5 alone fit
  at $-12.7$, rows 6--10 at $-18.9$); the Base row is excluded as a
  non-fine-tuned reference (the same rule as our own fits), and the fit is
  filter-dependent on that exclusion. Compare relative degradation from base,
  \emph{not} raw values,
  against ours: CLoRA's base BBH ($34.91$) $\neq$ our answer-only base ($33.10$).}
  \label{tab:clora}
\end{table*}
% Provenance: paper/writing/data/clora_table4_extracted.md (10-row extraction +
% reproduction snippet; fit verified r=-0.9805, slope -14.65 on 2026-07-11).

% ===========================================================================
\section{Reproducibility and Compute}\label{app:repro}
% ===========================================================================
Because our method \emph{is} a sweep, the run count is part of the claim. The
complete Llama-2-7B commonsense evidence is the $8\times7$ learning-rate grid = 56 cells, of
which 49 are clean (CorDA's 7 cells withheld; see Limitations); the math
grid adds 35 pooled sweep cells (LoRA / LoRA{+}wd / DoRA / CLoRA / MiLoRA, 7 rates
each) plus the faithful-recipe block (Table~\ref{tab:math}), and the
Qwen2.5-7B replication adds 112 cells (the complete 49-cell
commonsense grid plus 63 assessed math cells, 56 of which enter the single-seed
math fit; two diverged math cells are excluded with disclosure). Each cell is a full
fine-tune followed by in-process evaluation on the retention and adaptation suites.
All numbers are computed from a single cleaned registry
(\texttt{campaign\_summary\_clean.jsonl}), which spans every campaign---both
domains, both models, and auxiliary sweeps (e.g.\ the rank sweep)---and contains
\textbf{320 clean rows} after deduplication (359 raw rows $\to$ 343 unique by latest
\texttt{evaluated\_at}, then the residual-save explosion rows and withheld CorDA
cells removed); the raw registry is retained, labeled superseded, for provenance.
The Qwen math cells ingested on 2026-07-14 are computed from the live registry
(622 rows, 606 unique after deduplication), which supersedes the cleaned
snapshot for those fits.
The 320-row total is larger than the 49+35+112 headline sweep cells because it includes the
auxiliary and superseded-but-provenanced runs.

\paragraph{Estimator and registry mapping.} The magnitude axis of
Equation~\ref{eq:fdelta} is the registry field \texttt{fdelta}, computed
in-process at evaluation time by forward pre-hooks on every adapted module (100
prompts drawn evenly from the seven commonsense evaluation sets, batch 8,
max length 256; tokens with $\lVert x\rVert_2 \le 10^{-6}$ masked). The standalone
estimator additionally logs \texttt{fdelta\_matrix\_mean} (the per-matrix-mean
variant of the same statistic) and the spectral measures
\texttt{dw\_sv\_mean}/\texttt{dw\_sv\_max} compared in Figure~\ref{fig:axis}. For
residual-initialized adapters (MiLoRA, SC-LoRA, CorDA), $\Delta W_\ell$ includes
the initialization residual via a rank-$2r$ conversion at save time, so a $0$-step
run measures $\Delta W = 0$ (self-check passed for all released arms).

\paragraph{Companion experiments.} One observation cited in
Section~\ref{sec:law} (the weight-SVD leakage-direction
result $r\!\approx\!-0.09$) comes from a companion experiment that predates the
released registry; it is retained in the project's experiment archive, is not
recomputable from \texttt{campaign\_summary\_clean.jsonl}, and is used only as a
consistency check, never as a headline number. The residual rank effect in
observation~(i) is, by contrast, recomputed directly from the registry's
rank-sweep cells (\texttt{lora\_r4}--\texttt{r256} and
\texttt{mtx\_lora\_r8}--\texttt{r128}, which carry \texttt{retention\_mean} and
\texttt{fdelta}): partial $r\!=\!-0.69$ to $-0.74$ in raw rank, $-0.41$ with
method identity controlled, and sign-unstable under a log-rank specification.
The SVD-alignment drift battery, computed from the 320 saved adapter weights,
recovers each method's initialization design from its \emph{trained} adapter
(e.g.\ MiLoRA is the only arm whose update energy leans toward the minor singular
subspace), which we use as a fingerprinting diagnostic---not as a competing
predictive axis, since direction in the weight-SVD basis is uninformative for
retention (Section~\ref{sec:law}, observation~(ii)).

\paragraph{Efficiency.} The practical corollary of Section~\ref{sec:pareto} extends
to compute. LoRA{+}wd requires no initialization pass and trains in wall-clock
indistinguishable from plain LoRA (a $\sim\!0.1\%$ difference in our logs), because
weight decay is a decoupled AdamW flag rather than a structural change. The other
arms pay strictly more for the same trainable-parameter budget. DoRA is
$\approx\!2.1\times$ slower---its magnitude--direction decomposition runs every
step---for no retention or adaptation benefit. The data-aware inits (MiLoRA, PiSSA,
LoRA-Null, SC-LoRA, CorDA) pay a one-time initialization tax---weight SVDs
(MiLoRA/PiSSA) or calibration forward passes plus an eigendecomposition or damped
inverse (LoRA-Null/SC-LoRA/CorDA), CorDA++ the most expensive at
$\approx\!5\times$ CorDA---after which they train as vanilla LoRA. CLoRA pays at
train time: it holds a frozen orthogonality-projection block resident in memory whose
size scales with $k$---analytically $0.42$\,GB at $k{=}128$, $1.67$\,GB at the default
$k{=}512$, up to $6.7$\,GB at $k{=}2048$ (a projection: our swept maximum is
$k{=}1024$, $\approx\!3.3$\,GiB; these are analytical resident sizes, and peak GPU
memory was not instrumented)---plus a measured $\sim\!15\%$ per-step overhead
(14--17\% in our training logs; $1.17\times$ at $k{=}1024$). On the retention
frontier, then, the subspace-free magnitude knob is also the cheapest.

\paragraph{Compute.} All runs used a single 8-GPU NVIDIA B200 node with one
training-plus-evaluation cell per GPU. From the scheduler's occupancy over the
campaign ($\sim$9 days of wall-clock for $\sim$288 cells across both models and
domains, plus documented losses to scheduler collisions and retry storms), we
estimate the total at \textbf{$\sim$1{,}700--2{,}000 B200-GPU-hours} (estimate
derived from cell counts $\times$ per-cell times, not metered).
%TODO(data): add a public repro pointer (config registry + campaign_summary.jsonl)
%at submission.

\end{document}
