% Claims x evidence x pending-control map (E6). Hand-maintained; keep in sync with
% Limitations. Experiment codes: B4 = eval-matched calibration + sensitivity arm
% (LANDED for SC-LoRA, 4 cells 07-09/10; CorDA/LoRA-Null expansion queued);
% B5a = param-matched LoRA+wd control (LANDED 07-12/13: r16 control + plain-LoRA
% rank ladder; the r16-collapse-basin seed replicate is queued);
% B5c = seeds 43/44 on headline cells (LANDED: s43 x7, s44 x7 — DoRA s44 landed);
% C5 = base-ceiling calibration (LANDED for Llama-2 07-12: core 25.89, broad
% 35.26; Qwen ceilings still open); Qwen math = the Qwen2.5-7B math grid
% (LANDED 2026-07-14, qualitative: n=56 s42 cells, BBH-only, r=-0.70).
\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{5pt}
\caption{\textbf{What each claim rests on, and the named experiment that closes its
gap.} Evidence strength is stated at the level the current data supports.}
\label{tab:claims}
\begin{tabular}{p{0.30\textwidth} p{0.34\textwidth} p{0.28\textwidth}}
\toprule
Claim & Evidence strength & Pending control \\
\midrule
1. Magnitude law (mechanism) & complete 49-cell grid; $r\!=\!-0.86$/$-0.92$; LOMO-predictive; headline cells three-seed (B5c landed, all seven adapters); Qwen math replicates qualitatively ($n\!=\!56$, BBH-only, $r\!=\!-0.70$) & Qwen commonsense seeds; Qwen base ceilings \\
\quad geometry inert given $\dw$ & on-curve intercepts and slopes jointly n.s.; full-pool $F$ SC-LoRA-driven, seed-42-only & seed-averaged ANCOVA recompute \\
\quad SC-LoRA deviation & resolved: seeds 43/44 ($22.5\!\to\!25.6/25.8$) and B4 eval-matched calibration ($\to\!26.5$--$27.0$) both return it to the curve; three-seed math cell on the law (residual $-1.8$\,pp) & extend B4 arm to CorDA/LoRA-Null \\
2. LR-artifact diagnosis & ingredients shown from our own sweep; seed half landed (B5c) & reproduce-a-published-win \\
3. LoRA{+}wd corollary & edges plain LoRA outside three-seed noise on both axes; matches/edges the elaborate adapters; 6/7 safe rates; B5a landed: r16 control reproduces the operating point at half the parameters, rank ladder moves along the curve & r16-collapse-basin seed replicate; wd knob for the other frontier arms \\
\quad CorDA placement & withheld (calibration bug; re-run in progress) & CorDA nq\_open re-run + B4; CorDA++ \\
Broad-retention texture & calibrated (C5 landed: broad ceiling 35.26); TruthfulQA immunity real (constant $\approx\!-3$\,pp offset, not a floor) & Qwen base ceilings \\
Second model (Qwen2.5-7B) & commonsense replicates in full ($n\!=\!49$, $r\!=\!-0.86$; single seed); math replicates qualitatively ($n\!=\!56$, BBH-only, $r\!=\!-0.70$, slope $-14.4$\,pp/decade) & Qwen commonsense seeds; Qwen base ceilings \\
\bottomrule
\end{tabular}
\end{table*}
